{
  if (callback == null) {
    ServletOutputStream output=asyncContext.getResponse().getOutputStream();
    output.write(buffer,offset,length);
    if (output.isReady())     complete();
  }
 else {
    complete();
  }
}
