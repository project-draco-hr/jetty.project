{
  int needed=0;
  int len=s.length();
  for (int i=0; i < len; i++) {
    char c=s.charAt(i);
    if (c >= 128)     throw new IllegalArgumentException();
    needed+=CODES[c][1];
  }
  return (needed + 7) / 8;
}
