{
  TR[] trs={new TR(200,null,-1,null,false),new TR(200,null,-1,CONTENT,false),new TR(200,null,CONTENT.length(),null,true),new TR(200,null,CONTENT.length(),CONTENT,false),new TR(200,"text/html",-1,null,true),new TR(200,"text/html",-1,CONTENT,false),new TR(200,"text/html",CONTENT.length(),null,true),new TR(200,"text/html",CONTENT.length(),CONTENT,false)};
  List<Run[]> data=new ArrayList<>();
  for (  TR tr : trs) {
    for (int v=9; v <= 11; v++) {
      for (int chunks=1; chunks <= 6; chunks++) {
        for (        ConnectionType connection : ConnectionType.values()) {
          if (connection.isSupportedByHttp(v)) {
            data.add(Run.as(tr,v,chunks,connection));
          }
        }
      }
    }
  }
  return data;
}
