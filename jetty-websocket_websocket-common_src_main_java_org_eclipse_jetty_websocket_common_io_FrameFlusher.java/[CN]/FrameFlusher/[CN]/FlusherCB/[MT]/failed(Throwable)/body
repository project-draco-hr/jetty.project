{
synchronized (lock) {
    succeeded.addAll(active);
    active.clear();
  }
  for (  FrameEntry frame : succeeded) {
    frame.notifyFailed(x);
    frame.freeBuffers();
  }
  succeeded.clear();
  super.failed(x);
  onFailure(x);
}
