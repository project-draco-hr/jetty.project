{
synchronized (lock) {
    succeeded.addAll(active);
    active.clear();
  }
  for (  FrameEntry frame : succeeded) {
    frame.notifySucceeded();
    frame.freeBuffers();
  }
  super.succeeded();
}
