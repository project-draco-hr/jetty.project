{
  for (  FrameEntry entry : entries) {
    notifyCallbackSuccess(entry.callback);
    entry.release();
  }
  entries.clear();
  if (releaseAggregate)   BufferUtil.clearToFill(aggregate);
  super.succeeded();
}
