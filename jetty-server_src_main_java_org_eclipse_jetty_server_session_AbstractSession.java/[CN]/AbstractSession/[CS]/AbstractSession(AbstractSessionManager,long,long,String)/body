{
  _manager=abstractSessionManager;
  _created=created;
  _clusterId=clusterId;
  _nodeId=_manager._sessionIdManager.getNodeId(_clusterId,null);
  _accessed=accessed;
  _lastAccessed=accessed;
  _requests=1;
  if (LOG.isDebugEnabled())   LOG.debug("new session " + _nodeId + " "+ _clusterId);
}
