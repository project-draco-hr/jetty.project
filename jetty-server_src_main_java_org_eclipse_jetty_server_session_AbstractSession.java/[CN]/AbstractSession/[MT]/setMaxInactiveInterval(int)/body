{
  if (secs <= 0)   LOG.warn("Session {} is now immortal (maxInactiveInterval={})",_clusterId,secs);
 else   if (LOG.isDebugEnabled())   LOG.debug("Session {} maxInactiveInterval={}",_clusterId,secs);
  _maxIdleMs=(long)secs * 1000L;
}
