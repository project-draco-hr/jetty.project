{
  send(info,content,lastContent,streamBlocker.getPhase(),streamBlocker);
  try {
    streamBlocker.block();
  }
 catch (  IOException e) {
    throw e;
  }
catch (  Exception e) {
    throw new EofException(e);
  }
}
