{
  System.err.println("SSL sOut");
  LOG.debug("{} shutdownOutput",_session);
  if (_oshut)   return;
  try {
    _oshut=true;
    _engine.closeOutbound();
    process(null,null);
  }
 catch (  IOException e) {
    LOG.ignore(e);
    close();
  }
}
