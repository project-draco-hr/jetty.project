{
  if (_closing)   return;
  _closing=true;
  LOG.debug("{} close",_session);
  try {
    _engine.closeOutbound();
    process(null,null);
  }
 catch (  IOException e) {
    LOG.ignore(e);
  }
 finally {
    super.close();
  }
}
