{
  LOG.debug("{} shutdownInput",_session);
  if (_ishut)   return;
  _ishut=true;
  try {
    _engine.closeInbound();
    process(null,null);
  }
 catch (  IOException e) {
    LOG.ignore(e);
    close();
  }
}
