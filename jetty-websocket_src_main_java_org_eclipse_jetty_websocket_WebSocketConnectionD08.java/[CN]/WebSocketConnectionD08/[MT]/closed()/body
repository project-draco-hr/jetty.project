{
  final boolean closed;
synchronized (this) {
    closed=_closeCode == 0;
    if (closed)     _closeCode=WebSocketConnectionD08.CLOSE_NOCLOSE;
  }
  if (closed)   _webSocket.onClose(WebSocketConnectionD08.CLOSE_NOCLOSE,"closed");
}
