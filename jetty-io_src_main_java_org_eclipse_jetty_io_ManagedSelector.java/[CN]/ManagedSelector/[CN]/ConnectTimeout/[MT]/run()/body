{
  SocketChannel channel=connect.channel;
  if (channel.isConnectionPending()) {
    if (SelectorManager.LOG.isDebugEnabled())     SelectorManager.LOG.debug("Channel {} timed out while connecting, closing it",channel);
    connect.failed(new SocketTimeoutException());
  }
}
