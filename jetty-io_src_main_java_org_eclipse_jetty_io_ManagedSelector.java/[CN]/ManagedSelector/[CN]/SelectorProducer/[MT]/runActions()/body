{
  while (true) {
    Runnable action;
    try (SpinLock.Lock lock=_lock.lock()){
      action=_actions.poll();
      if (action == null) {
        _selecting=true;
        return null;
      }
    }
     if (action instanceof Product)     return action;
    runChange(action);
  }
}
