{
  if (SelectorManager.LOG.isDebugEnabled())   SelectorManager.LOG.debug("Stopping {}",this);
  Stop stop=new Stop();
  submit(stop);
  stop.await(getStopTimeout());
  if (SelectorManager.LOG.isDebugEnabled())   SelectorManager.LOG.debug("Stopped {}",this);
}
