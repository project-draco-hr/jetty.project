{
  try {
    if (SelectorManager.LOG.isDebugEnabled())     SelectorManager.LOG.debug("Running change {}",change);
    change.run();
  }
 catch (  Throwable x) {
    SelectorManager.LOG.debug("Could not run change " + change,x);
  }
}
