{
  if (_state != State.HEADER)   throw new IllegalStateException("STATE!=START");
  _method=null;
  _status=status;
  _reason=reason;
  if (_reason != null) {
    if (_reason.length() > 1024)     _reason=reason.substring(0,1024);
    _reason=_reason.replace('\r','?').replace('\n','?');
  }
}
