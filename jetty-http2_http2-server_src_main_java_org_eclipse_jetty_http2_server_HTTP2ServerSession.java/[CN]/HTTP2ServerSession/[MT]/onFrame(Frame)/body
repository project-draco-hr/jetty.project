{
switch (frame.getType()) {
case PREFACE:
    onPreface();
  break;
case SETTINGS:
onSettings((SettingsFrame)frame,false);
break;
case HEADERS:
onHeaders((HeadersFrame)frame);
break;
default :
super.onFrame(frame);
break;
}
}
