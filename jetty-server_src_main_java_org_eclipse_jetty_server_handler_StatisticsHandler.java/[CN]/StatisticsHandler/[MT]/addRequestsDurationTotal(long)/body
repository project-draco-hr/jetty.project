{
synchronized (this) {
    _requestsDurationTotal+=duration;
    if (_requestsDurationMin == 0 || duration < _requestsDurationMin)     _requestsDurationMin=duration;
    if (duration > _requestsDurationMax)     _requestsDurationMax=duration;
  }
}
