{
  if (!_connection.isIncluding()) {
    _connection.getResponseFields().putLongField(name,value);
    if (HttpHeader.CONTENT_LENGTH.is(name))     _connection._generator.setContentLength(value);
  }
}
