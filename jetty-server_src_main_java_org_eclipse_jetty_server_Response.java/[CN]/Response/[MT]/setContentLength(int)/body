{
  if (isCommitted() || _processor.isIncluding())   return;
  _contentLength=len;
  _fields.putLongField(HttpHeader.CONTENT_LENGTH.toString(),(long)len);
  if (_contentLength > 0)   checkAllContentWritten(_processor.getOutputStream().getWritten());
}
