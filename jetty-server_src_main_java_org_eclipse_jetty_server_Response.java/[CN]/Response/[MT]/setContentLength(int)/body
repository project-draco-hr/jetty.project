{
  if (isCommitted() || _channel.isIncluding())   return;
  _contentLength=len;
  _fields.putLongField(HttpHeader.CONTENT_LENGTH.toString(),(long)len);
  if (_contentLength > 0)   checkAllContentWritten(_channel.getOutputStream().getWritten());
}
