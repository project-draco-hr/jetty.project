{
  aborted=true;
  if (client.provideDestination(scheme(),host(),port()).abort(this,reason))   return true;
  HttpConversation conversation=client.getConversation(conversation());
  return conversation.abort(reason);
}
