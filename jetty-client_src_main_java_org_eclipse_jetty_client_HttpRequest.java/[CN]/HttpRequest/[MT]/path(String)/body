{
  URI uri=URI.create(path);
  this.path=uri.getRawPath();
  String query=uri.getRawQuery();
  if (query != null) {
    this.query=query;
    extractParams(query);
  }
  this.uri=buildURI();
  return this;
}
