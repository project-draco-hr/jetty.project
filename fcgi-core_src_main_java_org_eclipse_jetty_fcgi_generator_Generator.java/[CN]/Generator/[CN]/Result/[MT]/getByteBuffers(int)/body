{
  int newLength=length + (buffer == null ? 0 : 1);
  ByteBuffer[] result;
  result=previous != null ? previous.getByteBuffers(newLength) : new ByteBuffer[newLength];
  if (buffer != null)   result[result.length - newLength]=buffer;
  return result;
}
