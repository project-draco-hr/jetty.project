{
synchronized (this) {
    if (_dispatched) {
      _redispatched=true;
    }
 else {
      _dispatched=_manager.dispatch(_handler);
      if (!_dispatched) {
        Log.warn("Dispatched Failed!");
        updateKey();
      }
    }
  }
}
