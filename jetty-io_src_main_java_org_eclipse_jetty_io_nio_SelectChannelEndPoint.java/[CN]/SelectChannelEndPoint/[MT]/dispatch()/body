{
synchronized (this) {
    if (_dispatched) {
      _redispatched=true;
      return true;
    }
    _dispatched=_manager.dispatch((Runnable)this);
    if (!_dispatched)     Log.warn("Dispatched Failed!");
    return _dispatched;
  }
}
