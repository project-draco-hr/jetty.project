{
  try {
    SelectionKey key=_key;
    if (key != null)     key.cancel();
  }
 catch (  Throwable e) {
    LOG.ignore(e);
  }
  try {
    super.close();
  }
 catch (  IOException e) {
    LOG.ignore(e);
  }
 finally {
    updateKey();
  }
}
