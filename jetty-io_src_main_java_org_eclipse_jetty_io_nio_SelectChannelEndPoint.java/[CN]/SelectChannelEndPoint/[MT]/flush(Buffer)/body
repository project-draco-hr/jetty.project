{
  int l=super.flush(buffer);
  if (l == 0 && buffer != null && buffer.hasContent()) {
synchronized (this) {
      _writable=false;
      if (!_dispatched)       updateKey();
    }
  }
 else   if (l > 0) {
    _writable=true;
    notIdle();
  }
  return l;
}
