{
  int l=super.flush(buffer);
  if (l == 0 && buffer != null && buffer.hasContent()) {
synchronized (this) {
      if (_dispatched)       _writable=false;
    }
  }
 else   if (l > 0) {
    _writable=true;
    notIdle();
  }
  return l;
}
