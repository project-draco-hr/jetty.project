{
  int l=super.gather(buffers);
  if (l == 0) {
    for (    ByteBuffer b : buffers) {
      if (b.hasRemaining()) {
synchronized (this) {
          if (_dispatched)           _writable=false;
        }
      }
    }
  }
 else   if (l > 0) {
    _writable=true;
    notIdle();
  }
  return l;
}
