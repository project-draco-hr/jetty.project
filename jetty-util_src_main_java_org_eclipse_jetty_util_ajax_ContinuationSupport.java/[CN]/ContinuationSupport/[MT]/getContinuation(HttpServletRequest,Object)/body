{
  Continuation continuation=(Continuation)request.getAttribute("org.eclipse.jetty.ajax.Continuation");
  if (continuation == null)   continuation=new WaitingContinuation(mutex);
 else   continuation.setMutex(mutex);
  return continuation;
}
