{
  if (index < __staticTable.length)   return __staticTable[index];
  int d=_headerTable.size() - index + __staticTable.length - 1;
  if (d >= 0)   return _headerTable.getUnsafe(d);
  return null;
}
