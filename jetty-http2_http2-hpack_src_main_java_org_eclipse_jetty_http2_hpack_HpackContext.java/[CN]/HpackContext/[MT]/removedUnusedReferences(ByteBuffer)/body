{
  Entry entry=_refSet._refSetNext;
  while (entry != _refSet) {
    Entry next=entry._refSetNext;
    if (entry.isUsed())     entry._used=false;
 else {
      buffer.put((byte)0x80);
      NBitInteger.encode(buffer,7,index(entry));
      entry.removeFromRefSet();
    }
    entry=next;
  }
}
