{
  if (LOG.isDebugEnabled())   LOG.debug(String.format("RefSet[%x] cleared",hashCode()));
  Entry entry=_refSet._refSetNext;
  while (entry != _refSet) {
    Entry next=entry._refSetNext;
    entry.removeFromRefSet();
    entry=next;
  }
}
