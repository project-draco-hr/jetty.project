{
  if (streamId < 0)   throw new IllegalArgumentException("Invalid stream id: " + streamId);
  int maxFrameSize=getMaxFrameSize();
  encoder.encode(metaData,lease,maxFrameSize);
  long length=lease.getTotalLength();
  if (length > maxFrameSize)   throw new IllegalArgumentException("Invalid headers, too big");
  int flags=Flag.END_HEADERS;
  if (!contentFollows)   flags|=Flag.END_STREAM;
  ByteBuffer header=generateHeader(lease,FrameType.HEADERS,(int)length,flags,streamId);
  BufferUtil.flipToFlush(header,0);
  lease.prepend(header,true);
}
