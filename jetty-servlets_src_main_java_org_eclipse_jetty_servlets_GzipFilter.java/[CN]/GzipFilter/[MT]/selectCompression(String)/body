{
  if (encodingHeader != null) {
    if (encodingHeader.toLowerCase().contains(GZIP))     return GZIP;
    if (encodingHeader.toLowerCase().contains(DEFLATE))     return DEFLATE;
  }
  return null;
}
