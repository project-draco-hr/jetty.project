{
  Set<Edge> fromedges=new HashSet<Edge>();
  for (  Edge edge : this.edges) {
    if ((edge.getFrom() == node) || (edge.getTo() == node)) {
      fromedges.add(edge);
    }
  }
  return fromedges;
}
