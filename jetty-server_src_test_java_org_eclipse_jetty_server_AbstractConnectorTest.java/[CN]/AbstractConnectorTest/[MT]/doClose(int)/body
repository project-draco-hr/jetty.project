{
  for (int idx=0; idx < count; idx++) {
    if (_out[idx] != null)     _out[idx].close();
    if (_in[idx] != null)     _in[idx].close();
    if (_socket[idx] != null)     _socket[idx].close();
  }
  _closed.await();
}
