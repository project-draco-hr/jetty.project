{
  this.baseDir=new SearchDir(JETTY_BASE);
  this.homeDir=new SearchDir(JETTY_HOME);
  this.homeDir.setDir(homeDir);
  this.baseDir.setDir(homeDir);
  if (baseDir != null) {
    this.baseDir.setDir(baseDir);
  }
}
