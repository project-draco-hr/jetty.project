{
  if (buffer.remaining() < length)   throw new IllegalArgumentException();
  int position=buffer.position();
  for (  Chunk chunk : chunks) {
    buffer.put(chunk.buffer,chunk.offset,chunk.length);
  }
  BufferUtil.flipToFlush(buffer,position);
}
