{
  StringBuffer msg=new StringBuffer();
  Iterator<?> it=coll.iterator();
  while (it.hasNext()) {
    msg.append(String.valueOf(it.next()));
    if (it.hasNext()) {
      msg.append(delim);
    }
  }
  return msg.toString();
}
