{
  if (filePath == null)   return null;
  if ((filePath.startsWith("\"") || filePath.startsWith("'")) && (filePath.endsWith("\"") || filePath.endsWith("'")))   return filePath.substring(1,filePath.length() - 1);
  return filePath;
}
