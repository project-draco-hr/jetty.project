{
  HttpClient client=new HttpClient();
  client.start();
  client.setFollowRedirects(false);
  ContentResponse response=client.newRequest("localhost",8080).followRedirects(true).send().get(5,TimeUnit.SECONDS);
  Assert.assertEquals(200,response.getStatus());
}
