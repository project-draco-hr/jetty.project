{
synchronized (SslConnection.this) {
    return _endp.isInputShutdown() && !(_unwrapBuf != null && BufferUtil.hasContent(_unwrapBuf)) && !(_inbound != null && BufferUtil.hasContent(_inbound));
  }
}
