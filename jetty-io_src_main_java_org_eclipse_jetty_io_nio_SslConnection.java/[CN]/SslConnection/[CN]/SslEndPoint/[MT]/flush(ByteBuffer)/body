{
  int len=0;
  for (  ByteBuffer b : buffers) {
    if (b.hasRemaining()) {
      int l=b.remaining();
      process(null,b);
      l=l - b.remaining();
      if (l > 0)       len+=l;
 else       break;
    }
  }
  return len;
}
