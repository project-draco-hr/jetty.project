{
synchronized (this) {
    if (--_allocations == 0) {
      if (_buffers != null && _inbound.remaining() == 0 && _outbound.remaining() == 0 && _unwrapBuf.remaining() == 0) {
        _inbound=null;
        _outbound=null;
        _unwrapBuf=null;
        __buffers.set(_buffers);
        _buffers=null;
      }
    }
  }
}
