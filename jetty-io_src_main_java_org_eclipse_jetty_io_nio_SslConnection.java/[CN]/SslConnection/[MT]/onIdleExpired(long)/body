{
  try {
    LOG.debug("onIdleExpired {}ms on {}",idleForMs,this);
    if (_endp.isOutputShutdown())     _sslEndPoint.close();
 else     _sslEndPoint.shutdownOutput();
  }
 catch (  IOException e) {
    LOG.warn(e);
    super.onIdleExpired(idleForMs);
  }
}
