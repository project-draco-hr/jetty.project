{
  if (flusher.isFailed()) {
    if (callback != null)     callback.writeFailed(new ZipException());
    return;
  }
  FrameEntry entry=new FrameEntry(frame,callback);
  LOG.debug("Queuing {}",entry);
  entries.offer(entry);
  flusher.iterate();
}
