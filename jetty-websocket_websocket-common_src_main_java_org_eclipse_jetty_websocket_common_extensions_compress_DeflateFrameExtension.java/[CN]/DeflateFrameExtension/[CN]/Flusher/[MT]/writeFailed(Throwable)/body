{
  getBufferPool().release(payload);
  notifyCallbackFailure(current.callback,x);
  failed(x);
  FrameEntry entry;
  while ((entry=entries.poll()) != null)   notifyCallbackFailure(entry.callback,x);
}
