{
  if (LOG.isDebugEnabled())   LOG.debug("Idle timeout {}ms expired on {}",getEndPoint().getIdleTimeout(),this);
  getSession().close(ErrorCodes.NO_ERROR,"idle_timeout",closeCallback);
  return false;
}
