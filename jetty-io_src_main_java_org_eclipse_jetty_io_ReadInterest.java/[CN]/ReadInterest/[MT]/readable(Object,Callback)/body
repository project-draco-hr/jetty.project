{
  if (!_interested.compareAndSet(false,true))   throw new ReadPendingException();
  _readContext=context;
  _readCallback=callback;
  try {
    if (readIsPossible())     readable();
  }
 catch (  IOException e) {
    failed(e);
  }
}
