{
  if (!_interested.compareAndSet(false,true))   throw new ReadPendingException();
  _context=context;
  _callback=callback;
  try {
    if (registerReadInterest())     readable();
  }
 catch (  IOException e) {
    failed(e);
  }
}
