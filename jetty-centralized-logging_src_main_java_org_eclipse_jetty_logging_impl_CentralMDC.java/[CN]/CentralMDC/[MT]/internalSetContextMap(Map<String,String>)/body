{
  if (local == null) {
    return;
  }
  HashMap<String,String> map=local.get();
  if (map == null) {
    map=new HashMap<String,String>();
    local.set(map);
  }
 else {
    map.clear();
  }
  map.putAll(contextMap);
}
