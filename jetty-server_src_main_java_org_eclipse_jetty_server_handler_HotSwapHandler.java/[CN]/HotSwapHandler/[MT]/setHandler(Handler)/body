{
  if (handler == null)   throw new IllegalArgumentException("Parameter handler is null.");
  try {
    updateBean(_handler,handler);
    _handler=handler;
    Server server=getServer();
    handler.setServer(server);
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}
