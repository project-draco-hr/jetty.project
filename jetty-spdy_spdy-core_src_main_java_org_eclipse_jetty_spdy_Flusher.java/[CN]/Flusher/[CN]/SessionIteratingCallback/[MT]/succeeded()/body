{
  if (LOG.isDebugEnabled()) {
synchronized (lock) {
      LOG.debug("Completed write of {}, {} frame(s) in queue",active,queue.size());
    }
  }
  for (  FrameBytes frame : active)   frame.succeeded();
  active.clear();
  super.succeeded();
}
