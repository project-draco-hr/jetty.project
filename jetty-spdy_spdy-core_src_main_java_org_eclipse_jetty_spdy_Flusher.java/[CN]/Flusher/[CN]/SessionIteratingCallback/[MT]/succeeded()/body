{
  if (LOG.isDebugEnabled()) {
synchronized (queue) {
      LOG.debug("Completed write of {}, {} frame(s) in queue",active,queue.size());
    }
  }
  active.succeeded();
synchronized (queue) {
    flushing=false;
  }
  super.succeeded();
}
