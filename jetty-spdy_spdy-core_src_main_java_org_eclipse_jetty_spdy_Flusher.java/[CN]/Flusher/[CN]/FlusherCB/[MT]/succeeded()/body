{
synchronized (lock) {
    if (LOG.isDebugEnabled())     LOG.debug("Succeeded write of {}, q={}",active,queue.size());
    succeeded.addAll(active);
    active.clear();
  }
  for (  FrameBytes frame : succeeded)   frame.succeeded();
  super.succeeded();
}
