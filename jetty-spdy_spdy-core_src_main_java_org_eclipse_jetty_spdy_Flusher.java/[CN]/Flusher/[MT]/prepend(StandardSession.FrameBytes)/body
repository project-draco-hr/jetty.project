{
  Throwable failure;
synchronized (queue) {
    failure=this.failure;
    if (failure == null) {
      int index=0;
      while (index < queue.size()) {
        StandardSession.FrameBytes element=queue.get(index);
        if (element.compareTo(frameBytes) <= 0)         break;
        ++index;
      }
      queue.add(index,frameBytes);
    }
  }
  if (failure == null)   iteratingCallback.iterate();
 else   frameBytes.failed(new SPDYException(failure));
}
