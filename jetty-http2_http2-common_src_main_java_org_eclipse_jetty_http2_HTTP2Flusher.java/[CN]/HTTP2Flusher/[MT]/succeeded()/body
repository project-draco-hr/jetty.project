{
  lease.recycle();
  for (int i=0; i < actives.size(); ++i)   completes.add(actives.get(i));
  actives.clear();
  if (LOG.isDebugEnabled())   LOG.debug("Written {} frames for {}",completes.size(),completes);
  while (!completes.isEmpty()) {
    Entry entry=completes.poll();
    entry.succeeded();
  }
  super.succeeded();
}
