{
  Queue<Entry> queued;
synchronized (this) {
    queued=new ArrayDeque<>(frames);
    frames.clear();
  }
  if (LOG.isDebugEnabled())   LOG.debug("Aborting, queued={}",queued.size());
  for (  Entry entry : queued)   closed(entry,x);
  session.abort(x);
}
