{
switch (version) {
case 2:
    priority<<=6;
  break;
case 3:
priority<<=5;
break;
default :
throw new StreamException(StreamStatus.UNSUPPORTED_VERSION);
}
buffer.put(priority);
buffer.put((byte)0);
}
