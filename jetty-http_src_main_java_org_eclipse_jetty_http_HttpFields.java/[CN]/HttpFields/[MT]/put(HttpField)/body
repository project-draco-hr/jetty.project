{
  boolean put=false;
  for (int i=size(); i-- > 0; ) {
    HttpField f=get(i);
    if (f.isSameName(field)) {
      if (put)       remove(i);
 else {
        set(i,field);
        put=true;
      }
    }
  }
  if (!put)   add(field);
}
