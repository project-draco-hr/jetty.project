{
  boolean put=false;
  for (int i=_fields.size(); i-- > 0; ) {
    HttpField f=_fields.get(i);
    if (f.isSame(field)) {
      if (put)       _fields.remove(i);
 else       _fields.set(i,field);
    }
  }
  _fields.add(field);
}
