{
  Field field=getField(name);
  if (field == null)   return null;
  final int revision=_revision;
  final List<String> list=new ArrayList<String>();
  while (field != null) {
    if (field._revision == revision)     list.add(field.getValue());
    field=field._next;
  }
  return list;
}
