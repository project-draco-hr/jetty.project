{
  final List<String> list=new ArrayList<String>(_fields.size());
  final int revision=_revision;
  for (  Field f : _fields) {
    if (f != null && f._prev == null && f._revision == revision)     list.add(BufferUtil.to8859_1_String(f._name));
  }
  return list;
}
