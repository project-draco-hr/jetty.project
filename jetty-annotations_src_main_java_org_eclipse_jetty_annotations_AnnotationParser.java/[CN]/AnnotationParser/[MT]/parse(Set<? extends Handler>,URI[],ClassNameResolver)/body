{
  if (uris == null)   return;
  for (  URI uri : uris) {
    try {
      parse(handlers,uri,resolver);
    }
 catch (    Exception e) {
      LOG.warn("Problem parsing classes from {}",uri);
    }
  }
}
