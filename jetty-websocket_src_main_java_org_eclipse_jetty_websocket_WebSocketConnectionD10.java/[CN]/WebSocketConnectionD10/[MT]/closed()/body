{
  final boolean closed;
synchronized (this) {
    closed=_close == 0;
    if (closed)     _close=WebSocketConnectionD10.CLOSE_NOCLOSE;
  }
  if (closed)   _webSocket.onClose(WebSocketConnectionD10.CLOSE_NOCLOSE,"closed");
}
