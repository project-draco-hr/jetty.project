{
  _lock.lock();
  try {
    for (    ByteBuffer b : _writeBuffers) {
      process(b);
      if (b.hasRemaining())       return;
    }
    writeCompleted();
  }
 catch (  IOException e) {
    writeFailed(e);
  }
 finally {
    _lock.unlock();
  }
}
