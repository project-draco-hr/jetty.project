{
  if (softClose()) {
    getHttpDestination().close(this);
    getEndPoint().shutdownOutput();
    if (LOG.isDebugEnabled())     LOG.debug("Shutdown {}",this);
    getEndPoint().close();
    if (LOG.isDebugEnabled())     LOG.debug("Closed {}",this);
    abort(failure);
  }
}
