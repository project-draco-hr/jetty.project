{
  setMinThreads(minThreads);
  setMaxThreads(maxThreads);
  setIdleTimeout(idleTimeout);
  setStopTimeout(5000);
  if (queue == null)   queue=new BlockingArrayQueue<>(_minThreads,_minThreads);
  _jobs=queue;
}
