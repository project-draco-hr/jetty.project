{
  setMinThreads(minThreads);
  setMaxThreads(maxThreads);
  setIdleTimeout(idleTimeout);
  setStopTimeout(5000);
  if (queue == null)   queue=new ConcurrentArrayBlockingQueue.Unbounded<Runnable>();
  _jobs=queue;
}
