{
synchronized (_lock) {
    if (_callback != null)     throw new IllegalStateException();
    _callback=callback;
    _context=context;
    if (_done) {
      if (_complete)       dispatchCompleted();
 else       dispatchFailed();
    }
  }
}
