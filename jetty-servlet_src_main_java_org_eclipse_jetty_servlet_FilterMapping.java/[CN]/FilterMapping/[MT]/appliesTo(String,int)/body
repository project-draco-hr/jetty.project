{
  if (appliesTo(type)) {
    String[] pathSpecs=getPathSpecs();
    for (int i=0; i < pathSpecs.length; i++)     if (pathSpecs[i] != null && PathMap.match(pathSpecs[i],path,true))     return true;
  }
  return false;
}
