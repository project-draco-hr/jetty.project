{
  if (outbound == null) {
    return;
  }
  try {
    @SuppressWarnings("unused") Future<WriteResult> future=outbound.write(message);
  }
 catch (  IOException e) {
    LOG.log(Level.WARNING,"unable to echo message: " + message,e);
  }
}
