{
  if (app == null)   return null;
  if (!(app instanceof BundleApp))   throw new IllegalStateException(app + " is not a BundleApp");
  ContextHandler h=((BundleApp)app).getContextHandler();
  return h;
}
