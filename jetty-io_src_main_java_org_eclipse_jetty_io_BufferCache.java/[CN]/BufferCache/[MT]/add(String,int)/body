{
  CachedBuffer buffer=new CachedBuffer(value,ordinal);
  _stringMap.put(value,buffer);
  if (ordinal >= 0) {
    while ((ordinal - _index.size()) >= 0)     _index.add(null);
    _index.set(ordinal,buffer);
  }
  return buffer;
}
