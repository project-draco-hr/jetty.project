{
  byte[] array=buffer.isReadOnly() ? null : buffer.array();
  Map.Entry<String,Object> entry=_stringMap.getBestEntry(buffer);
  if (entry != null)   return (CachedBuffer)entry.getValue();
  return null;
}
