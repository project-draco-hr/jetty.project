{
synchronized (lock()) {
    try {
      while (!isFinished()) {
        T item=getNextContent();
        if (item == null)         blockForContent();
 else         consume(item,remaining(item));
      }
    }
 catch (    IOException e) {
      throw new RuntimeIOException(e);
    }
  }
}
