{
synchronized (lock()) {
    boolean empty=_inputQ.isEmpty();
    _inputQ.add(item);
    if (empty) {
      if (!onAsyncRead())       lock().notify();
    }
    LOG.debug("{} queued {}",this,item);
  }
}
