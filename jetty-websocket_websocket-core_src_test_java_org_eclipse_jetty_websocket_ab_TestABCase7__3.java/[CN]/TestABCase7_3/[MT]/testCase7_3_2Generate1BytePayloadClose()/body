{
  WebSocketFrame closeFrame=FrameBuilder.close().payload(new byte[]{0x00}).asFrame();
  Generator generator=new Generator(policy);
  ByteBuffer actual=ByteBuffer.allocate(32);
  generator.generate(actual,closeFrame);
}
