{
  for (  Request.Listener listener : request.listeners())   notifyFailure(listener,request,failure);
  for (  Request.Listener listener : client.getRequestListeners())   notifyFailure(listener,request,failure);
}
