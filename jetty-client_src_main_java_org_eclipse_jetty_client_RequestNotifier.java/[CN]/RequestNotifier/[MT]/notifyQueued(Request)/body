{
  for (  Request.Listener listener : request.getListeners())   notifyQueued(listener,request);
  for (  Request.Listener listener : client.getRequestListeners())   notifyQueued(listener,request);
}
