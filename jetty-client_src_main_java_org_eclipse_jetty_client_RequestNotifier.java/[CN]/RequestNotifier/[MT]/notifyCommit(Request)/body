{
  for (  Request.CommitListener listener : request.getRequestListeners(Request.CommitListener.class))   notifyCommit(listener,request);
  for (  Request.Listener listener : client.getRequestListeners())   notifyCommit(listener,request);
}
