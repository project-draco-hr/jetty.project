{
  for (  Request.Listener listener : request.getListeners())   notifyHeaders(listener,request);
  for (  Request.Listener listener : client.getRequestListeners())   notifyHeaders(listener,request);
}
