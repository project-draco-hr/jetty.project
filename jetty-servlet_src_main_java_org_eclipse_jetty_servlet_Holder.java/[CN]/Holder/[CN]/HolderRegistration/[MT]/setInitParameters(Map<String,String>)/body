{
  illegalStateIfContextStarted();
  for (  String name : initParameters.keySet())   if (Holder.this.getInitParameter(name) != null)   return false;
  Holder.this.setInitParameters(initParameters);
  return true;
}
