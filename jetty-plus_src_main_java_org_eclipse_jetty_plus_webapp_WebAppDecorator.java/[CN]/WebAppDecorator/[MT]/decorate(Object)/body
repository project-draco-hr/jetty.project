{
  if (_runAses != null)   _runAses.setRunAs(o);
  if (_injections != null)   _injections.inject(o);
  if (_callbacks != null) {
    try {
      _callbacks.callPostConstructCallback(o);
    }
 catch (    Exception e) {
      throw new ServletException(e);
    }
  }
}
