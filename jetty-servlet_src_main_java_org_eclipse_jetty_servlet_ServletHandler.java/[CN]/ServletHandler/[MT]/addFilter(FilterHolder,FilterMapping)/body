{
  if (filter != null)   setFilters(LazyList.addToArray(getFilters(),filter,FilterHolder.class));
  if (filterMapping != null)   setFilterMappings(LazyList.addToArray(getFilterMappings(),filterMapping,FilterMapping.class));
}
