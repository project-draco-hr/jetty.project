{
  try {
    Main main=new Main();
    List<String> xmls=main.processCommandLine(args);
    if (xmls != null)     main.start(xmls);
  }
 catch (  UsageException e) {
    usageExit(e.getCause(),e.getExitCode());
  }
catch (  Throwable e) {
    usageExit(e,UsageException.ERR_UNKNOWN);
  }
}
