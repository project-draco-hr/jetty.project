{
  try {
    if (begun) {
      listener.onHeaders(headerParser.getRequest());
    }
 else {
      fields=null;
    }
  }
 catch (  Throwable x) {
    logger.debug("Exception while invoking listener " + listener,x);
  }
  return true;
}
