{
  int remaining=output.remaining();
  int length=Math.min(bytes.length - offset,remaining);
  output.put(bytes,offset,length);
  offset+=length;
  if (offset == bytes.length)   setConsumed(true);
  return length;
}
