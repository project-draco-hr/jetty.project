{
  final boolean closed;
synchronized (this) {
    closed=_closeCode == 0;
    if (closed)     _closeCode=WebSocketConnectionD13.CLOSE_NO_CLOSE;
  }
  if (closed)   _webSocket.onClose(WebSocketConnectionD13.CLOSE_NO_CLOSE,"closed");
}
