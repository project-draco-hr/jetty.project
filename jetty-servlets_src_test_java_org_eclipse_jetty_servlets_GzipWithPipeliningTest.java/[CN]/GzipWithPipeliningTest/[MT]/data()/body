{
  String[][] data=new String[][]{{CompressionType.GZIP.getEncodingHeader()},{CompressionType.DEFLATE.getEncodingHeader() + ", " + CompressionType.GZIP.getEncodingHeader()},{CompressionType.GZIP.getEncodingHeader() + ", " + CompressionType.DEFLATE.getEncodingHeader()},{CompressionType.DEFLATE.getEncodingHeader()}};
  return Arrays.asList(data);
}
