{
  Runnable runnable=new Runnable(){
    @Override public void run(){
      onFailed(context,x);
    }
  }
;
  if (_executor == null)   new Thread(runnable).start();
 else   _executor.execute(runnable);
}
