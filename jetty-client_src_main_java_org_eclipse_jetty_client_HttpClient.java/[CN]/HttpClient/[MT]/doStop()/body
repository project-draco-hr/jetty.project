{
  for (  HttpDestination destination : _destinations.values())   destination.close();
  _timeoutQ.cancelAll();
  _idleTimeoutQ.cancelAll();
  super.doStop();
  if (_threadPool instanceof LocalQueuedThreadPool)   removeBean(_threadPool);
  removeBean(_connector);
}
