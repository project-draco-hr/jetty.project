{
  if (_providers.remove(provider)) {
    removeBean(provider);
    if (getServer() != null)     getServer().getContainer().update(this,provider,null,"provider");
  }
  try {
    provider.stop();
  }
 catch (  Exception e) {
    Log.warn("Unable to stop Provider",e);
  }
}
