{
  List<File> classesDirs=new ArrayList<File>();
  if (useTestScope && (testClassesDirectory != null))   classesDirs.add(testClassesDirectory);
  if (getClassesDirectory() != null)   classesDirs.add(getClassesDirectory());
  return classesDirs;
}
