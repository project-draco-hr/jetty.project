{
  if (excluded != null) {
    _excludedUA=new HashSet<String>();
    StringTokenizer tok=new StringTokenizer(excluded,",",false);
    while (tok.hasMoreTokens())     _excludedUA.add(tok.nextToken());
  }
}
