{
  Pattern[] ps=_excludedPathPatterns.toArray(new Pattern[_excludedPathPatterns.size()]);
  String[] s=new String[ps.length];
  int i=0;
  for (  Pattern p : ps)   s[i++]=p.toString();
  return s;
}
