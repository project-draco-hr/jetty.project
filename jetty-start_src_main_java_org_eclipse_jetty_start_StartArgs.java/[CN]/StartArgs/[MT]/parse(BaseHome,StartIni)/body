{
  String source;
  try {
    source=baseHome.toShortForm(ini.getFile());
  }
 catch (  Exception e) {
    throw new UsageException(ERR_BAD_ARG,"Bad file: %s",ini);
  }
  for (  String line : ini) {
    parse(line,source);
  }
}
