{
  if (server != null) {
    server.stop();
    server.join();
  }
  if (proxy != null) {
    proxy.stop();
    proxy.join();
  }
  factory.stop();
  ((StdErrLog)Log.getLogger(HttpChannel.class)).setHideStacks(false);
}
