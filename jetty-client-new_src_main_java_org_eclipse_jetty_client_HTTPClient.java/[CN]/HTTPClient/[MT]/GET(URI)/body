{
  boolean secure=false;
  String scheme=uri.getScheme();
  if ("https".equals(scheme))   secure=true;
 else   if (!"http".equals(scheme))   throw new IllegalArgumentException("Invalid scheme " + scheme);
  return builder(uri.getHost() + ":" + uri.getPort()).method("GET").secure(secure).path(uri.getPath()).agent(getUserAgent()).followRedirects(isFollowRedirects()).build().send();
}
