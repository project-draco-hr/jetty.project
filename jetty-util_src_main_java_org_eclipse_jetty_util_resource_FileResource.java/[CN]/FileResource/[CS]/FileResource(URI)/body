{
  File file=new File(uri);
  _file=file;
  _uri=normalizeURI(_file,uri);
  if (!_uri.equals(_file.toURI()) && !_uri.toString().equals(_file.toURI().toString()))   _alias=_file.toURI();
 else   _alias=checkAlias(_file);
}
