{
synchronized (this) {
    if (_exchange == exchange) {
      try {
        _destination.returnConnection(this,true);
      }
 catch (      IOException x) {
        LOG.ignore(x);
      }
    }
  }
}
