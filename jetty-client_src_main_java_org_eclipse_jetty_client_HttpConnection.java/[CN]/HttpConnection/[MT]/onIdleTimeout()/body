{
  if (LOG.isDebugEnabled())   LOG.debug("Idle timeout state {} - {}",idleTimeoutState,this);
  if (idleTimeoutState.compareAndSet(0,-1))   close(new TimeoutException("idle_timeout"));
  return false;
}
