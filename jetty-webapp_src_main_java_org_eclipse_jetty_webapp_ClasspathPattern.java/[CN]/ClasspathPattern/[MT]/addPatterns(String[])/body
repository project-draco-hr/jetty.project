{
  if (patterns != null) {
    if (_patterns == null) {
      setPatterns(patterns);
    }
 else {
      Entry entry=null;
      for (      String pattern : patterns) {
        entry=createEntry(pattern);
        if (entry != null) {
          _patterns.add(pattern);
          _entries.add(entry);
        }
      }
    }
  }
}
