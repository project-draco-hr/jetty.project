{
  boolean close=delegate.onIdleTimeout();
  if (multiplexed)   close&=isFillInterested();
  if (close)   close(new TimeoutException("Idle timeout " + getEndPoint().getIdleTimeout() + "ms"));
  return false;
}
