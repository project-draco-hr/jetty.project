{
  Collection<Part> parts=getParsedParts();
  MultiException err=new MultiException();
  for (  Part p : parts) {
    try {
      p.delete();
    }
 catch (    Exception e) {
      err.add(e);
    }
  }
  err.ifExceptionThrowMulti();
}
