{
  try {
    if (onlyOutput) {
      try {
        getEndPoint().shutdownOutput();
      }
 catch (      IOException x) {
        getEndPoint().close();
      }
    }
 else {
      getEndPoint().close();
    }
  }
 catch (  IOException x) {
    logger.trace("",x);
  }
}
