{
  if (data.indexOf("disconnect") >= 0)   _outbound.disconnect();
 else {
    for (    ChatWebSocket member : _members) {
      try {
        member._outbound.sendMessage(frame,data);
      }
 catch (      IOException e) {
        Log.warn(e);
      }
    }
  }
}
