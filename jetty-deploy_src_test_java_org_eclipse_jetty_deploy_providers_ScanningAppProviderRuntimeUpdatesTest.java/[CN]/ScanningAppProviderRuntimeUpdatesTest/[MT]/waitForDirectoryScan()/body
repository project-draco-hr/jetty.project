{
  int scan=_scans.get() + 2 * _providers;
  try {
    do {
      Thread.sleep(200);
    }
 while (_scans.get() < scan);
    Thread.sleep(200);
  }
 catch (  InterruptedException e) {
    Log.ignore(e);
  }
}
