{
  while (true) {
    State current=_interestState.get();
    if (LOG.isDebugEnabled())     LOG.debug("Changing interests in state {} for {}",current,this);
switch (current) {
case UPDATE_PENDING:
case UPDATED:
{
        if (!_interestState.compareAndSet(current,State.LOCKED))         continue;
        try {
          int oldInterestOps=_interestOps;
          int newInterestOps=oldInterestOps | operation;
          if (LOG.isDebugEnabled())           LOG.debug("changeInterests s={} {}->{} for {}",current,oldInterestOps,newInterestOps,this);
          if (newInterestOps != oldInterestOps)           _interestOps=newInterestOps;
          if (current == State.UPDATED)           _selector.submit(_runUpdateKey);
        }
  finally {
          _interestState.set(State.UPDATE_PENDING);
        }
        return;
      }
case LOCKED:
{
      Thread.yield();
      break;
    }
default :
{
    throw new IllegalStateException("Invalid state: " + current);
  }
}
}
}
