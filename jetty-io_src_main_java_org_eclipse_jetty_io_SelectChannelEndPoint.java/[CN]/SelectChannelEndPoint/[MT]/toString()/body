{
  String keyString="";
  if (_key.isValid()) {
    if (_key.isReadable())     keyString+="r";
    if (_key.isWritable())     keyString+="w";
  }
 else {
    keyString+="!";
  }
  return String.format("%s{io=%d,k=%s}",_interestOps,keyString);
}
