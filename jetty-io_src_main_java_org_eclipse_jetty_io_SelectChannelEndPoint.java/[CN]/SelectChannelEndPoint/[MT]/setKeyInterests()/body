{
  int oldInterestOps=_key.interestOps();
  int newInterestOps=_interestOps;
  if (LOG.isDebugEnabled())   LOG.debug("Key interests update {} -> {}",oldInterestOps,newInterestOps);
  if (oldInterestOps != newInterestOps)   _key.interestOps(newInterestOps);
}
