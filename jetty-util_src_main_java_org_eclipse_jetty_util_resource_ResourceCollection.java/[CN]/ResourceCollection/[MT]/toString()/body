{
  if (_resources == null)   throw new IllegalStateException("*resources* not set.");
  StringBuilder buffer=new StringBuilder();
  for (  Resource r : _resources)   buffer.append(r.toString()).append(';');
  return buffer.toString();
}
