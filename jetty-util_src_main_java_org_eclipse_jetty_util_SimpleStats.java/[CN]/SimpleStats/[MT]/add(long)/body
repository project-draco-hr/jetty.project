{
  if (_curr.get() != -1) {
    long curr=_curr.addAndGet(delta);
    if (_min.get() != -1)     updateMin(curr);
    if (_max.get() != -1)     updateMax(curr);
  }
  if (_total.get() != -1 && delta < 0)   _total.addAndGet(abs(delta));
}
