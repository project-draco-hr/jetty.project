{
  if (o == null)   return null;
  if (!(o instanceof ServletHolder))   return null;
  ServletHolder holder=(ServletHolder)o;
  String className=holder.getClassName();
  return (RunAs)_runAsMap.get(className);
}
