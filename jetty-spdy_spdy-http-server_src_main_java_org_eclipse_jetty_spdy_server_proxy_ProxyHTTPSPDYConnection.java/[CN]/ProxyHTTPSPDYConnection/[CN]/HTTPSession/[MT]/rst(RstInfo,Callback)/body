{
  MetaData.Response info=new MetaData.Response(HttpVersion.fromString(headers.get("version").getValue()),502,"SPDY reset received from upstream server",null,0);
  send(info,false,null,true,Callback.Adapter.INSTANCE);
}
