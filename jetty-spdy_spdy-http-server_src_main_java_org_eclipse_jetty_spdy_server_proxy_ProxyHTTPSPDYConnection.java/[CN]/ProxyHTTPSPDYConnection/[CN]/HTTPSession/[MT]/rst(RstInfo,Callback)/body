{
  HttpGenerator.ResponseInfo info=new HttpGenerator.ResponseInfo(HttpVersion.fromString(headers.get("version").value()),null,0,502,"SPDY reset received from upstream server",false);
  send(info,null,true,new Callback.Adapter());
}
