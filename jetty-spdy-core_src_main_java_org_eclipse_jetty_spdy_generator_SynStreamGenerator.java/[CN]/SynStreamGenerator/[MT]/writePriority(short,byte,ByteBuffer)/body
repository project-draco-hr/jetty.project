{
switch (version) {
case SPDY.V2:
    priority<<=6;
  break;
case SPDY.V3:
priority<<=5;
break;
default :
throw new StreamException(StreamStatus.UNSUPPORTED_VERSION);
}
buffer.put(priority);
buffer.put((byte)0);
}
