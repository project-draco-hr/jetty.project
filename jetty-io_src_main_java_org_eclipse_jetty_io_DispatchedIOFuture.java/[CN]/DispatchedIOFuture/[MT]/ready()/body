{
  _lock.lock();
  try {
    if (_complete)     throw new IllegalStateException();
    _ready=true;
    _complete=true;
    if (_callback != null)     dispatchCompleted();
    _block.signal();
  }
  finally {
    _lock.unlock();
  }
}
