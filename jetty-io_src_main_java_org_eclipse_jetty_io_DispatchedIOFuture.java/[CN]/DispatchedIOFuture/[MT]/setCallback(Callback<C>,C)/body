{
  _lock.lock();
  try {
    if (_callback != null)     throw new IllegalStateException();
    _callback=callback;
    _context=context;
    if (_complete) {
      if (_ready)       dispatchCompleted();
 else       dispatchFailed();
    }
  }
  finally {
    _lock.unlock();
  }
}
