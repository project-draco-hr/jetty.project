{
  _lock.lock();
  try {
    if (_done)     throw new IllegalStateException("complete",cause);
    _cause=cause;
    _done=true;
    if (_callback != null)     dispatchFailed();
    _block.signal();
  }
  finally {
    _lock.unlock();
  }
}
