{
  _lock.lock();
  try {
    if (_complete)     throw new IllegalStateException("complete",cause);
    _cause=cause;
    _complete=true;
    if (_callback != null)     dispatchFailed();
    _block.signal();
  }
  finally {
    _lock.unlock();
  }
}
