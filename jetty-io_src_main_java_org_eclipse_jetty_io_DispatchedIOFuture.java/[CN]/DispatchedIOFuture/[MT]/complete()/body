{
  _lock.lock();
  try {
    if (_done)     throw new IllegalStateException();
    _complete=true;
    _done=true;
    if (_callback != null)     dispatchCompleted();
    _block.signal();
  }
  finally {
    _lock.unlock();
  }
}
