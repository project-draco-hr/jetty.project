{
  super.init();
  String value=getInitParameter(SCRIPT_PATTERN_INIT_PARAM);
  if (value == null)   value="(.+?\\.php)";
  scriptPattern=Pattern.compile(value);
  fcgiHTTPS=Boolean.parseBoolean(getInitParameter(FASTCGI_HTTPS_INIT_PARAM));
}
