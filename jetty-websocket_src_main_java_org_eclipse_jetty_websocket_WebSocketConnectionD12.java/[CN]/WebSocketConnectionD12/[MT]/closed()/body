{
  final boolean closed;
synchronized (this) {
    closed=_closeCode == 0;
    if (closed)     _closeCode=WebSocketConnectionD12.CLOSE_NOCLOSE;
  }
  if (closed)   _webSocket.onClose(WebSocketConnectionD12.CLOSE_NOCLOSE,"closed");
}
