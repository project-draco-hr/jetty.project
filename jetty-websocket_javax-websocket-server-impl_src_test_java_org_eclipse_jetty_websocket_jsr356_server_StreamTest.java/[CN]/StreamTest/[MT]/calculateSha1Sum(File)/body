{
  MessageDigest digest=MessageDigest.getInstance("SHA1");
  try (FileInputStream fis=new FileInputStream(file);NoOpOutputStream noop=new NoOpOutputStream();DigestOutputStream digester=new DigestOutputStream(noop,digest)){
    IO.copy(fis,digester);
    return Hex.asHex(digest.digest());
  }
 }
