{
  if (LOG.isDebugEnabled()) {
    String reason=tryConvertPayload(frame.getPayload());
    if (LOG.isDebugEnabled())     LOG.debug("Received {}: {}/'{}'",frame.getType(),frame.getError(),reason);
  }
  flusher.close();
  notifyClose(this,frame);
  return false;
}
