{
switch (closed.get()) {
case NOT_CLOSED:
{
      close(ErrorCodes.NO_ERROR,"idle_timeout",Callback.Adapter.INSTANCE);
      break;
    }
case LOCALLY_CLOSED:
case REMOTELY_CLOSED:
{
    terminate();
    break;
  }
default :
{
  break;
}
}
}
