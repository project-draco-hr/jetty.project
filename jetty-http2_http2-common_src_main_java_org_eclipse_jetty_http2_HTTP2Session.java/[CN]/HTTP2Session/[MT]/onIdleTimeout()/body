{
switch (closed.get()) {
case NOT_CLOSED:
{
      return notifyIdleTimeout(this);
    }
case LOCALLY_CLOSED:
case REMOTELY_CLOSED:
{
    abort(new TimeoutException());
    return false;
  }
default :
{
  return false;
}
}
}
