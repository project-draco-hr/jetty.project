{
switch (closed.get()) {
case NOT_CLOSED:
{
      close(ErrorCode.NO_ERROR.code,"idle_timeout",Callback.Adapter.INSTANCE);
      break;
    }
case LOCALLY_CLOSED:
case REMOTELY_CLOSED:
{
    terminate();
    break;
  }
default :
{
  break;
}
}
}
