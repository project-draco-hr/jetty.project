{
switch (frame.getType()) {
case RST_STREAM:
{
      if (stream != null)       removeStream(stream,true);
      break;
    }
case GO_AWAY:
{
    disconnect();
    break;
  }
default :
{
  break;
}
}
callback.succeeded();
}
