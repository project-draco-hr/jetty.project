{
  super.close();
  Queue<FlusherEntry> queued;
synchronized (queue) {
    queued=new ArrayDeque<>(queue);
    queue.clear();
  }
  if (LOG.isDebugEnabled())   LOG.debug("Closing, queued={}",queued.size());
  for (  FlusherEntry item : queued)   closed(item);
}
