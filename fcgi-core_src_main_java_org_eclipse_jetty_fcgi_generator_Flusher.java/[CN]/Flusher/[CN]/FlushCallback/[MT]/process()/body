{
  Generator.Result result;
synchronized (queue) {
    if (queue.isEmpty()) {
      flushing=false;
      return false;
    }
    result=queue.poll();
  }
  active=result;
  ByteBuffer[] buffers=result.getByteBuffers();
  endPoint.write(this,buffers);
  return false;
}
