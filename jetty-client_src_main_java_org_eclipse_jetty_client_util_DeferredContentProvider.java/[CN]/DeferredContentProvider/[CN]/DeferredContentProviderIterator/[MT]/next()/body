{
synchronized (lock) {
    Chunk chunk=current=chunks.poll();
    if (chunk == CLOSE) {
      chunks.add(0,CLOSE);
      throw new NoSuchElementException();
    }
    return chunk == null ? null : chunk.buffer;
  }
}
