{
  if (_maxCachedFiles > 0 && _cachedFiles.get() >= _maxCachedFiles)   return;
  Miss miss=new Miss(pathInContext,resource);
  if (_cache.putIfAbsent(pathInContext,miss) != null)   miss.invalidate();
}
