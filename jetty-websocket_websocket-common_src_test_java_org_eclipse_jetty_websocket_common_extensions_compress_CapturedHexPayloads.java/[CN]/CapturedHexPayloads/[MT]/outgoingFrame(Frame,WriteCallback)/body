{
  String hexPayload=Hex.asHex(frame.getPayload());
  LOG.debug("outgoingFrame({}: \"{}\", {})",OpCode.name(frame.getOpCode()),hexPayload,callback != null ? callback.getClass().getSimpleName() : "<null>");
  captured.add(hexPayload);
  if (callback != null) {
    callback.writeSuccess();
  }
}
