{
  LOG.debug("ishut {}",this);
  if (_channel.isOpen()) {
    if (_channel instanceof SocketChannel) {
      Socket socket=((SocketChannel)_channel).socket();
      if (!socket.isInputShutdown())       socket.shutdownInput();
      if (socket.isOutputShutdown())       close();
    }
 else     close();
  }
}
