{
  if (LOG.isDebugEnabled()) {
    LOG.debug("HTTP2 Response #{}: {} {}content bytes",stream.getId(),lastContent ? "last " : "",content.remaining());
  }
  DataFrame frame=new DataFrame(stream.getId(),content,lastContent);
  stream.data(frame,callback);
}
