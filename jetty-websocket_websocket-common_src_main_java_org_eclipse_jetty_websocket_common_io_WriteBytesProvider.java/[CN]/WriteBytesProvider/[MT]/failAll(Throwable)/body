{
synchronized (this) {
    if (isFailed()) {
      return;
    }
    failure=t;
    for (    FrameEntry fe : queue) {
      notifyFailure(fe.callback);
    }
    queue.clear();
    flushCallback.failed(failure);
  }
}
