{
synchronized (this) {
    if (active != null) {
      active.notifyFailure(t);
    }
    failure=t;
    for (    FrameEntry fe : queue) {
      fe.notifyFailure(t);
    }
    queue.clear();
    flushCallback.failed(t);
  }
}
