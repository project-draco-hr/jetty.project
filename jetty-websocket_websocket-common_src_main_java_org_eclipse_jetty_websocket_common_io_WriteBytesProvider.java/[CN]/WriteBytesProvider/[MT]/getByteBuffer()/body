{
synchronized (this) {
    if (active == null) {
      if (queue.isEmpty()) {
        return null;
      }
      active=queue.pop();
    }
    if (active == null) {
      return null;
    }
    buffer=active.getByteBuffer();
  }
  return buffer;
}
