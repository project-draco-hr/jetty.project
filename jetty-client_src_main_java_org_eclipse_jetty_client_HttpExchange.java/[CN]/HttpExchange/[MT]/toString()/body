{
  try (SpinLock.Lock lock=_lock.lock()){
    return String.format("%s@%x req=%s/%s res=%s/%s",HttpExchange.class.getSimpleName(),hashCode(),requestTerminated,requestFailure,responseTerminated,responseFailure);
  }
 }
