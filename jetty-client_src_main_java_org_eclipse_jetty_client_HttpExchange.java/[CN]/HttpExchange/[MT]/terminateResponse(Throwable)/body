{
  try (SpinLock.Lock lock=_lock.lock()){
    responseState=State.TERMINATED;
    responseFailure=failure;
    if (State.TERMINATED.equals(requestState))     return new Result(getRequest(),requestFailure,getResponse(),responseFailure);
  }
   return null;
}
