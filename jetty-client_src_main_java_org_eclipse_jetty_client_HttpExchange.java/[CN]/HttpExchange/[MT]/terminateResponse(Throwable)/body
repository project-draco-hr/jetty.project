{
  try (SpinLock.Lock lock=_lock.lock()){
    responseTerminated=Boolean.TRUE;
    responseFailure=failure;
    if (Boolean.TRUE.equals(requestTerminated))     return new Result(getRequest(),requestFailure,getResponse(),responseFailure);
  }
   return null;
}
