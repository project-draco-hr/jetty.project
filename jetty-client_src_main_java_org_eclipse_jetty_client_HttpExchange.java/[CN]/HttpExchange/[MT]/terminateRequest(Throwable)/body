{
  try (SpinLock.Lock lock=_lock.lock()){
    requestTerminated=Boolean.TRUE;
    requestFailure=failure;
    if (Boolean.TRUE.equals(responseTerminated))     return new Result(getRequest(),requestFailure,getResponse(),responseFailure);
  }
   return null;
}
