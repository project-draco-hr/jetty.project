{
  if (scheme != null) {
    if (HttpScheme.HTTP.equalsIgnoreCase(scheme))     setScheme(HttpScheme.HTTP_BUFFER);
 else     if (HttpScheme.HTTPS.equalsIgnoreCase(scheme))     setScheme(HttpScheme.HTTPS_BUFFER);
 else     setScheme(new ByteArrayBuffer(scheme));
  }
}
