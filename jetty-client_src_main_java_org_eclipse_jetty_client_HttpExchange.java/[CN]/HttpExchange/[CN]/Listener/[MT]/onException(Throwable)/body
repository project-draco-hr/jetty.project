{
  try {
    if (ex instanceof SocketTimeoutException || ex.getCause() instanceof SocketTimeoutException) {
      Log.debug(ex);
      HttpExchange.this.onExpire();
    }
 else     HttpExchange.this.onException(ex);
  }
  finally {
    done();
  }
}
