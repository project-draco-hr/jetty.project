{
  _generator.addFrame(frame,content,_maxIdleTimeMs);
  _generator.flush();
  if (!_generator.isBufferEmpty() && _endp instanceof AsyncEndPoint)   ((AsyncEndPoint)_endp).scheduleWrite();
  _idle.access(_endp);
}
