{
  if (!(o instanceof MetaData))   return false;
  MetaData m=(MetaData)o;
  List<HttpField> lm=m.getFields();
  int s=0;
  for (  HttpField field : this) {
    s++;
    if (!lm.contains(field))     return false;
  }
  if (s != lm.size())   return false;
  return true;
}
