{
  int nbits=0xFF >>> (8 - n);
  i=i - nbits;
  if (i < 0)   return n == 8 ? 1 : 0;
  if (i == 0)   return n == 8 ? 2 : 1;
  int lz=Integer.numberOfLeadingZeros(i);
  int log=32 - lz;
  return (log + 6) / 7;
}
