{
  Response response=client.newRequest("localhost",connector.getLocalPort()).path("/303/127.0.0.1/302/localhost/done").send().get(5,TimeUnit.SECONDS);
  Assert.assertNotNull(response);
  Assert.assertEquals(200,response.status());
  Assert.assertFalse(response.headers().containsKey(HttpHeader.LOCATION.asString()));
}
