{
  int progress=parseNext();
  int total=progress > 0 ? 1 : 0;
  while (!isComplete() && _buffer != null && _buffer.length() > 0) {
    progress=parseNext();
    if (progress > 0)     total++;
  }
  return total;
}
