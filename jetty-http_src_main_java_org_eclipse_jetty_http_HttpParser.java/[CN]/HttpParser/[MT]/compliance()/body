{
  String compliance=System.getProperty(__COMPLIANCE);
  if (compliance != null)   return Compliance.valueOf(compliance);
  Boolean strict=Boolean.getBoolean(__STRICT);
  return strict ? Compliance.STRICT : Compliance.RFC7230;
}
