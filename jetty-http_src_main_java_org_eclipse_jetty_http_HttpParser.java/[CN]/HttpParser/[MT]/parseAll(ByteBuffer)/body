{
  if (_state == State.END)   reset();
  if (_state != State.START)   throw new IllegalStateException("!START");
  while (_state != State.END && buffer.hasRemaining()) {
    int remaining=buffer.remaining();
    parseNext(buffer);
    if (remaining == buffer.remaining())     break;
  }
}
