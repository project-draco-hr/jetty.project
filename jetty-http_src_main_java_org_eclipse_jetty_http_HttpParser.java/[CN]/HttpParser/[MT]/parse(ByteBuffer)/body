{
  if (_state == STATE_END)   reset();
  if (_state != STATE_START)   throw new IllegalStateException("!START");
  while (_state != STATE_END && buffer.hasRemaining())   if (!parseNext(buffer))   return;
}
