{
  if (_expect100Continue) {
    if (_in.available() == 0) {
      if (_response.isCommitted())       throw new IllegalStateException("Committed before 100 Continues");
      commitResponse(HttpGenerator.CONTINUE_100_INFO,null);
    }
    _expect100Continue=false;
  }
  return _in;
}
