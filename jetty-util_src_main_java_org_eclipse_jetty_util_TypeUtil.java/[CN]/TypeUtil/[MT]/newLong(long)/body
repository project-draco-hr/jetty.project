{
  if (i >= 0 && i < longCacheSize) {
    if (longCache[(int)i] == null)     longCache[(int)i]=new Long(i);
    return longCache[(int)i];
  }
 else   if (i == -1)   return minusOneL;
  return new Long(i);
}
