{
  if (bundle == null)   return false;
  BundleWebAppProvider deployer=(BundleWebAppProvider)_serviceTracker.getService();
  if (deployer != null)   return deployer.bundleAdded(bundle);
  return false;
}
