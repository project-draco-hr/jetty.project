{
  this.sslContextFactory=sslContextFactory;
  if (sslContextFactory != null)   addBean(sslContextFactory);
  if (listener != null)   addAsyncConnectionFactory(new ServerSPDY2AsyncConnectionFactory(listener));
}
