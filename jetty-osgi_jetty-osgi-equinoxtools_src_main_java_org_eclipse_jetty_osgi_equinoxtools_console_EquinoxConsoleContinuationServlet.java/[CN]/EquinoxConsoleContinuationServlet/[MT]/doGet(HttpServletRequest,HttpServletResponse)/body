{
  if (request.getParameter("action") != null)   doPost(request,response);
 else   response.sendRedirect(request.getContextPath() + request.getServletPath() + (request.getPathInfo() != null ? request.getPathInfo() : "")+ "/index.html");
}
