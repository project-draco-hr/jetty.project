{
  HttpConfiguration httpConfig=getHttpConfiguration();
  Integer mhls=frame.getSettings().get(SettingsFrame.MAX_HEADER_LIST_SIZE);
  if (mhls != null && mhls < httpConfig.getResponseHeaderSize())   LOG.warn("MAX_HEADER_LIST_SIZE<{} for {}",getHttpConfiguration().getResponseHeaderSize(),session);
}
