{
  if (_closed)   throw new EOFException();
  if (_aggregate == null)   _aggregate=_channel.getConnector().getByteBufferPool().acquire(_channel.getHttpConfiguration().getResponseBufferSize(),false);
  BufferUtil.append(_aggregate,(byte)b);
  _written++;
  if (!checkAllWritten() && BufferUtil.isFull(_aggregate))   _channel.write(_aggregate,false);
}
