{
  if (!_closed) {
    _closed=true;
    try {
      _channel.getResponse().closeOutput();
    }
 catch (    IOException e) {
      _channel.getEndPoint().shutdownOutput();
      LOG.ignore(e);
    }
    releaseBuffer();
  }
}
