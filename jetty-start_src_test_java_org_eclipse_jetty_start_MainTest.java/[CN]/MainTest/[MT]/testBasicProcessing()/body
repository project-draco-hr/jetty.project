{
  Main main=new Main();
  StartArgs args=main.processCommandLine(new String[]{"jetty.port=9090"});
  BaseHome baseHome=main.getBaseHome();
  System.err.println(args);
  ConfigurationAssert.assertConfiguration(baseHome,args,"assert-home.txt");
}
