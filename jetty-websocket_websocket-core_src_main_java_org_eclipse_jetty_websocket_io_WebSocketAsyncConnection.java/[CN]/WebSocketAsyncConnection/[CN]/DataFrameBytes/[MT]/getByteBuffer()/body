{
  try {
    int windowSize=policy.getBufferSize();
    size=frame.getPayloadLength();
    if (size > windowSize) {
      size=windowSize;
    }
    buffer=generator.generate(size,frame);
    return buffer;
  }
 catch (  Throwable x) {
    failed(context,x);
    return null;
  }
}
