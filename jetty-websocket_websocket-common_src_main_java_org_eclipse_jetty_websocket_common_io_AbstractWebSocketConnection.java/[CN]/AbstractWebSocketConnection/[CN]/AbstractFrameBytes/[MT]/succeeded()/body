{
  super.succeeded();
synchronized (queue) {
    if (LOG.isDebugEnabled()) {
      LOG.debug("Completed Write of {} ({} frame(s) in queue)",this,queue.size());
    }
    flushing=false;
  }
  complete();
}
