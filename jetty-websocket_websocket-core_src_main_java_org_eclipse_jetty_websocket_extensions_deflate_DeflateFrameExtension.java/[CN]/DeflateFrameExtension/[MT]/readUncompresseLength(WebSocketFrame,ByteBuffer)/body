{
  int length=data.get();
  int bytes=0;
  if (length == 0x7F) {
    length=0;
    bytes=8;
  }
 else   if (length == 0x7F) {
    length=0;
    bytes=2;
  }
  while (bytes > 0) {
    byte b=data.get();
    length|=(b & 0xFF) << (8 * bytes);
  }
  assertSanePayloadLength(frame,length);
  return length;
}
