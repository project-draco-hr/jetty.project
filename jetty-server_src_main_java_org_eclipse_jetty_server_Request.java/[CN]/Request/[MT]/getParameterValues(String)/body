{
  if (!_paramsExtracted)   extractParameters();
  if (_parameters == null)   restoreParameters();
  List<String> vals=_parameters.getValues(name);
  if (vals == null)   return null;
  return vals.toArray(new String[vals.size()]);
}
