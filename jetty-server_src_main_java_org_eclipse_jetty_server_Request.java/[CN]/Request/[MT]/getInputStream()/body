{
  if (_inputState != __NONE && _inputState != _STREAM)   throw new IllegalStateException("READER");
  _inputState=_STREAM;
  if (_channel.isExpecting100Continues())   _channel.continue100(_in.available());
  return _in;
}
