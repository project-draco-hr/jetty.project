{
  if (_serverPort <= 0 && _serverName == null)   getServerName();
  if (_serverPort <= 0) {
    if (getScheme().equalsIgnoreCase(URIUtil.HTTPS))     return 443;
    return 80;
  }
  return _serverPort;
}
