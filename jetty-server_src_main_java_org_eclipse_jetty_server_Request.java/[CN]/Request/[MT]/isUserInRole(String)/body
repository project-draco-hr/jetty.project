{
  if (_authentication instanceof Authentication.Deferred)   _authentication=((Authentication.Deferred)_authentication).authenticate();
  if (_authentication instanceof Authentication.User)   return ((Authentication.User)_authentication).isUserInRole(_scope,role);
  return false;
}
