{
  HttpChannelState continuation=getAsyncContinuation();
  if (continuation.isInitial() && !continuation.isAsyncStarted())   throw new IllegalStateException(continuation.getStatusString());
  return continuation;
}
