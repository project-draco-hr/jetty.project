{
  AsyncEndPoint endPoint=getEndPoint();
  while (true) {
    int filled=fill(endPoint,buffer);
    if (filled == 0) {
      break;
    }
 else     if (filled < 0) {
      close(false);
      break;
    }
 else {
      parser.parse(buffer);
    }
  }
}
