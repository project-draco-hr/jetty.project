{
  AsyncEndPoint endPoint=getEndPoint();
  while (true) {
    int filled=fill(endPoint,buffer);
    if (filled == 0) {
      return 0;
    }
 else     if (filled < 0) {
      close(false);
      return -1;
    }
 else {
      parser.parse(buffer);
    }
  }
}
