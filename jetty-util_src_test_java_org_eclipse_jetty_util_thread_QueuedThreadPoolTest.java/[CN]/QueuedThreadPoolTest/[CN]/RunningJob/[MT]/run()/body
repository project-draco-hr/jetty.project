{
  try {
    _latch.countDown();
    while (_latch.getCount() >= 2)     Thread.sleep(10);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
 finally {
    _jobs.incrementAndGet();
    _latch.countDown();
  }
}
