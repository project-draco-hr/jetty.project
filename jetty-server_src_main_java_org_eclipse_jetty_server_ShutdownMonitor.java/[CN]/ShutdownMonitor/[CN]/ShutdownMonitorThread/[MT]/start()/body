{
  if (isAlive()) {
    if (DEBUG)     System.err.printf("ShutdownMonitorThread already started");
    return;
  }
  startListenSocket();
  if (serverSocket == null) {
    return;
  }
  System.err.println("Starting ShutdownMonitorThread");
  super.start();
}
