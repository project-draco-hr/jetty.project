{
  for (  LifeCycle l : _lifeCycles) {
    try {
      if (l.isStarted()) {
        l.stop();
      }
      if ((l instanceof Destroyable) && destroy)       ((Destroyable)l).destroy();
    }
 catch (    Exception e) {
      debug(e);
    }
  }
}
