{
  try {
    HttpFields fields=_fields;
    _fields=new HttpFields(Math.max(10,fields.size() + 5));
    if (_method != null)     return new MetaData.Request(HttpVersion.HTTP_2,_scheme,_method,_authority,_path,fields);
    if (_status != 0)     return new MetaData.Response(HttpVersion.HTTP_2,_status,fields);
    return new MetaData(HttpVersion.HTTP_2,fields);
  }
  finally {
    _status=0;
    _method=null;
    _scheme=null;
    _authority=null;
    _path=null;
  }
}
