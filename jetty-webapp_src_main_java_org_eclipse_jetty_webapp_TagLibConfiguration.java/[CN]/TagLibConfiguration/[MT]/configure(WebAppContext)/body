{
  if (_processor == null) {
    if (Log.isDebugEnabled())     Log.debug("No TldProcessor configured, skipping tld processing");
    return;
  }
  for (  TldDescriptor d : _descriptors)   _processor.process(context,d);
}
