{
  XmlParser.Node root;
  try {
    root=_parser.parse(tld.getInputStream());
  }
 catch (  Exception e) {
    root=_parser.parse(tld.getURL().toString());
  }
  if (root == null) {
    Log.warn("No TLD root in {}",tld);
  }
 else   _roots.add(root);
  return root;
}
