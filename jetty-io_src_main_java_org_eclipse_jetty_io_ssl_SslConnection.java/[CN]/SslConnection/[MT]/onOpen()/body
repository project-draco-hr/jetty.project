{
  try {
    super.onOpen();
    _sslEngine.beginHandshake();
    if (_sslEngine.getUseClientMode())     getExecutor().execute(_runWriteEmpty);
  }
 catch (  SSLException x) {
    getEndPoint().close();
    throw new RuntimeIOException(x);
  }
}
