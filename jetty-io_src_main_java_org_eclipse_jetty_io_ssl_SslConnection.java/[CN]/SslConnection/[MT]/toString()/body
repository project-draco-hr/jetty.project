{
  return String.format("SslConnection@%x{%s,%s%s}",hashCode(),_sslEngine.getHandshakeStatus(),_decryptedEndPoint._readInterest.isInterested() ? "R" : "",_decryptedEndPoint._writeFlusher.isWritePending() ? "W" : "");
}
