{
  try {
    if (!uri.startsWith(_prefix))     return null;
    return new HttpURI(new URI(_proxyTo + uri.substring(_prefix.length())).normalize().toString());
  }
 catch (  URISyntaxException ex) {
    throw new MalformedURLException(ex.getMessage());
  }
}
