{
  Frame frame=entry.frame;
  if (OpCode.isControlFrame(frame.getOpCode())) {
    nextOutgoingFrame(frame,this);
    return;
  }
  if (!frame.hasPayload()) {
    nextOutgoingFrame(frame,this);
    return;
  }
  compress(frame,true);
}
