{
  _lock.lock();
  try {
    if (_state == IDLE)     throw new IllegalStateException("IDLE");
    if (_state == null)     LOG.debug("Blocker not complete",new Throwable());
  }
  finally {
    _state=IDLE;
    _idle.signalAll();
    _lock.unlock();
  }
}
