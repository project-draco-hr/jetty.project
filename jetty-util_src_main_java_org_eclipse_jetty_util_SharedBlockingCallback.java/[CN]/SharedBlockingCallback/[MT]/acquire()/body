{
  _lock.lock();
  try {
    while (_state != IDLE)     _idle.await();
    _state=null;
  }
 catch (  final InterruptedException e) {
    throw new InterruptedIOException(){
{
        initCause(e);
      }
    }
;
  }
 finally {
    _lock.unlock();
  }
}
