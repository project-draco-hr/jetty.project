{
  byte[] bytes;
  if (charEncoding == null)   bytes=s.getBytes(Charset.forName(StringUtil.__ISO_8859_1));
 else   bytes=s.getBytes(Charset.forName(charEncoding));
  return new String(encode(bytes));
}
