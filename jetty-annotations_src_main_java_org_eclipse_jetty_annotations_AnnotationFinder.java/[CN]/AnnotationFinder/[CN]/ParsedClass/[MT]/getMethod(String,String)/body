{
  Iterator<ParsedMethod> itor=methods.iterator();
  ParsedMethod method=null;
  while (itor.hasNext() && method == null) {
    ParsedMethod m=itor.next();
    if (m.matches(name,paramString))     method=m;
  }
  return method;
}
