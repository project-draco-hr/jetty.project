{
  super.complete();
  if (_activeThreads.decrementAndGet() == 0) {
    try {
      if (isValid()) {
        willPassivate();
        save(this);
        didActivate();
      }
    }
 catch (    Exception e) {
      LOG.warn("Problem saving session({})",getId(),e);
    }
  }
}
