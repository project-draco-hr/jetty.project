{
  if (info == null)   new ContentCallback(content,lastContent,callback).iterate();
 else {
    if (_channel.isExpecting100Continue())     _generator.setPersistent(false);
    new CommitCallback(info,content,lastContent,callback).iterate();
  }
}
