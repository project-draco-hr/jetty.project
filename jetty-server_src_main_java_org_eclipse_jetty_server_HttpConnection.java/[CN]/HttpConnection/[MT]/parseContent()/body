{
  if (LOG.isDebugEnabled())   LOG.debug("{} parseContent",this);
  boolean handled=false;
  while (_parser.inContentState()) {
    int filled=fillRequestBuffer();
    boolean handle=parseRequestBuffer();
    handled|=handle;
    if (handle || filled <= 0)     break;
  }
  return handled;
}
