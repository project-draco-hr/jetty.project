{
  if (_channel.isExpecting100Continue()) {
    _parser.reset();
    _parser.close();
  }
 else   if (_generator.isPersistent())   _parser.reset();
 else   _parser.close();
  _generator.reset();
  _channel.reset();
  releaseRequestBuffer();
}
