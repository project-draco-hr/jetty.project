{
  if (_generator.isIdle() && !_channel.getRequest().getAsyncContinuation().isSuspended()) {
    try {
      _endp.close();
    }
 catch (    IOException e) {
      LOG.debug(e);
    }
  }
  if (_parser.isIdle())   _parser.setPersistent(false);
}
