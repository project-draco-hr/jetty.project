{
  while (true) {
synchronized (_inputQ) {
      _inputQ.clear();
    }
    if (_parser.isComplete())     return;
    try {
      blockForContent();
    }
 catch (    IOException e) {
      LOG.warn(e);
    }
  }
}
