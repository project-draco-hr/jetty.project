{
  if (!this.exchange.compareAndSet(null,exchange))   throw new UnsupportedOperationException("Pipelined requests not supported");
  exchange.associate(this);
  LOG.debug("{} associated to {}",exchange,this);
}
