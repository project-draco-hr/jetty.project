{
  HttpExchange exchange=this.exchange.getAndSet(null);
  if (exchange != null)   exchange.disassociate(this);
  if (LOG.isDebugEnabled())   LOG.debug("{} disassociated from {}",exchange,this);
  return exchange;
}
