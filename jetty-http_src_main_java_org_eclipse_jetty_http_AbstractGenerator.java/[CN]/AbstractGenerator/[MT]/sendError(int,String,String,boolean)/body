{
  if (!isCommitted()) {
    setResponse(code,reason);
    if (close)     _persistent=false;
    completeHeader(null,false);
    if (content != null)     addContent(new View(new ByteArrayBuffer(content)),Generator.LAST);
    complete();
  }
}
