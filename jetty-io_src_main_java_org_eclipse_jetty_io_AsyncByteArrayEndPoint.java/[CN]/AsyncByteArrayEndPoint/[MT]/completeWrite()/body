{
  try {
    flush(_writeBuffers);
    for (    ByteBuffer b : _writeBuffers) {
      if (b.hasRemaining()) {
        return;
      }
    }
    _writeFuture.ready();
  }
 catch (  final IOException e) {
    _writeBuffers=null;
    if (!_writeFuture.isDone())     _writeFuture.fail(e);
  }
}
