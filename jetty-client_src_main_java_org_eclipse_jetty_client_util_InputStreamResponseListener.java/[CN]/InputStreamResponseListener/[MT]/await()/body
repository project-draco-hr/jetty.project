{
  try {
synchronized (this) {
      while (length.get() >= maxBufferSize && failure == null && !closed)       wait();
      return failure == null && !closed;
    }
  }
 catch (  InterruptedException x) {
    Thread.currentThread().interrupt();
    return false;
  }
}
