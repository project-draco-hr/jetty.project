{
  try {
synchronized (this) {
      if (length.get() >= maxBufferSize && failure == null && !closed)       wait();
      return failure == null && !closed;
    }
  }
 catch (  InterruptedException x) {
    return false;
  }
}
