{
  LOG.debug("Notify Binary Frame: {}",frame);
  for (  Listener listener : listeners) {
    try {
      listener.onBinaryFrame(frame);
    }
 catch (    Throwable t) {
      LOG.warn(t);
    }
  }
}
