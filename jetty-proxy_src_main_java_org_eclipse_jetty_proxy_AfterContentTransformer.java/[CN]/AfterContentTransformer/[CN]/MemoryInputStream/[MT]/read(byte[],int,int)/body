{
  if (len == 0)   return 0;
  if (index == buffers.size())   return -1;
  if (slice == null)   slice=buffers.get(index).slice();
  int size=Math.min(len,slice.remaining());
  slice.get(b,off,size);
  if (!slice.hasRemaining()) {
    ++index;
    slice=null;
  }
  return size;
}
