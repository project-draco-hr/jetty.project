{
  if (inputFile == null) {
    Path path=Files.createTempFile(getOverflowDirectory(),getInputFilePrefix(),null);
    inputFile=FileChannel.open(path,StandardOpenOption.CREATE,StandardOpenOption.READ,StandardOpenOption.WRITE,StandardOpenOption.DELETE_ON_CLOSE);
    int size=sourceBuffers.size();
    if (size > 0) {
      inputFile.write(sourceBuffers.toArray(new ByteBuffer[size]));
      sourceBuffers.clear();
    }
  }
  inputFile.write(input);
}
