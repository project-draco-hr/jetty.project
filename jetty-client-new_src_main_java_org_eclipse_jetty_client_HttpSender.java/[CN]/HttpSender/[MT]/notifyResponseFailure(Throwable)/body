{
  Response.Listener listener=conversation.listener();
  try {
    if (listener != null)     listener.onFailure(null,x);
  }
 catch (  Exception xx) {
    LOG.info("Exception while notifying listener " + listener,xx);
  }
}
