{
  Request request=conversation.request();
  Request.Listener listener=request.listener();
  try {
    if (listener != null)     listener.onFailure(request,x);
  }
 catch (  Exception xx) {
    LOG.info("Exception while notifying listener " + listener,xx);
  }
}
