{
  Task[] t=new Task[10];
  for (int i=0; i < t.length; i++) {
    t[i]=new Task();
    _produce.add(t[i]);
  }
  _produce.add(NULLTASK);
  _ewyk.execute();
  for (int i=0; i < t.length; i++)   assertThat(t[i].hasRun(),is(true));
  Assert.assertEquals(_ewyk,_executions.poll());
}
