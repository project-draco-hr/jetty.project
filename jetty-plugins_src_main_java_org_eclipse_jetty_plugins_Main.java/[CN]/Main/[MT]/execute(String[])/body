{
  parseEnvironmentVariables();
  parseCommandline(args);
  configureMavenService();
  _pluginManager=new DefaultPluginManager(_mavenService,_jettyHome);
  if (_listPlugins)   listPlugins();
 else   if (_installPlugin != null)   installPlugin();
}
