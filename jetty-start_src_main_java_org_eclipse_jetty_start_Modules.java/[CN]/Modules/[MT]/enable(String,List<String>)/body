{
  Module module=modules.get(name);
  if (module == null) {
    System.err.printf("WARNING: Cannot enable requested module [%s]: not a valid module name.%n",name);
    return;
  }
  module.setEnabled(true);
  if (sources != null)   module.addSources(sources);
}
