{
  this.connection=connection;
  if (_onConnect) {
    try {
      connection.write(null,fnf(),"sent on connect");
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
  connected.countDown();
}
