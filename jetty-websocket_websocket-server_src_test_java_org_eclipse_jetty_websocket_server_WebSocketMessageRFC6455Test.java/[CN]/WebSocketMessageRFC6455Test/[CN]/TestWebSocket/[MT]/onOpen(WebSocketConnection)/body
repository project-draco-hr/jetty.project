{
  this.connection=connection;
  if (_onConnect) {
    try {
      connection.write("sent on connect");
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
  connected.countDown();
}
