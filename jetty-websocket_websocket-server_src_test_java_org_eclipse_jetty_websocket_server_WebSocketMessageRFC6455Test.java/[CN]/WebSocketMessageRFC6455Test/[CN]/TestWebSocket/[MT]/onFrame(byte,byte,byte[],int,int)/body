{
  if (_echo) {
    OpCode op=OpCode.from(opcode);
switch (op) {
case CLOSE:
case PING:
case PONG:
      break;
default :
    try {
      connection.sendFrame(flags,opcode,data,offset,length);
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
}
}
return false;
}
