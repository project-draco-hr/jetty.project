{
  if (maxHeaderTableSize > _remoteMaxHeaderTableSize)   throw new IllegalArgumentException();
  buffer.put((byte)0x20);
  NBitInteger.encode(buffer,4,maxHeaderTableSize);
  _context.resize(maxHeaderTableSize);
}
