{
  try {
    while (true) {
      Runnable task=_producer.produce();
      if (task == null)       break;
      _executor.execute(task);
    }
  }
  finally {
    _producer.onProductionComplete();
  }
}
