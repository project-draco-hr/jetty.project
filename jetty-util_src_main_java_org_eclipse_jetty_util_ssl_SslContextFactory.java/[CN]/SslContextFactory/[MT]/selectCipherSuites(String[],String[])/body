{
  Set<String> selected_ciphers=new LinkedHashSet<String>();
  if (_includeCipherSuites != null) {
    for (    String cipherSuite : supportedCipherSuites)     if (_includeCipherSuites.contains(cipherSuite))     selected_ciphers.add(cipherSuite);
  }
 else   selected_ciphers.addAll(Arrays.asList(enabledCipherSuites));
  if (_excludeCipherSuites != null)   selected_ciphers.removeAll(_excludeCipherSuites);
  return selected_ciphers.toArray(new String[selected_ciphers.size()]);
}
