{
  Set<String> selected_ciphers=new LinkedHashSet<>();
  if (_includeCipherSuites != null) {
    for (    String cipherSuite : _includeCipherSuites)     if (Arrays.asList(supportedCipherSuites).contains(cipherSuite))     selected_ciphers.add(cipherSuite);
  }
 else   selected_ciphers.addAll(Arrays.asList(enabledCipherSuites));
  selected_ciphers.removeAll(_excludeCipherSuites);
  return selected_ciphers.toArray(new String[selected_ciphers.size()]);
}
