{
  if (this.connection == null) {
    LOG.debug("Not connected");
    return;
  }
  if (connection.isOpen() == false) {
    LOG.debug("Not open");
    return;
  }
  try {
    connection.write(message);
  }
 catch (  IOException e) {
    LOG.info(e);
  }
}
