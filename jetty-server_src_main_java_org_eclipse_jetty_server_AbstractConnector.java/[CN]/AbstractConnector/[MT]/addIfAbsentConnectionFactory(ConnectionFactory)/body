{
synchronized (_factories) {
    String key=factory.getProtocol();
    if (_factories.containsKey(key))     LOG.info("{} addIfAbsent ignored {}",this,factory);
 else {
      _factories.put(key,factory);
      addBean(factory);
      if (_defaultProtocol == null)       _defaultProtocol=factory.getProtocol();
      LOG.info("{} addIfAbsent added {}",this,factory);
    }
  }
}
