{
  if (isAccepting()) {
    if (previous == null)     LOG.warn(current);
 else     LOG.debug(current);
  }
 else {
    LOG.ignore(current);
  }
  try {
    Thread.sleep(1000);
    return true;
  }
 catch (  Throwable x) {
    return false;
  }
}
