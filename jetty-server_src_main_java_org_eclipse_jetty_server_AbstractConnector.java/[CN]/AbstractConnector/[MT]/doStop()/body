{
  for (  Thread thread : _acceptors) {
    if (thread != null) {
      thread.interrupt();
      long stopTimeout=getStopTimeout();
      if (stopTimeout > 0)       thread.join(stopTimeout);
    }
  }
  super.doStop();
  logger.info("Stopped {}",this);
}
