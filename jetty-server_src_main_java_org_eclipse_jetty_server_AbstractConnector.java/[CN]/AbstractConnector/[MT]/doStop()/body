{
  try {
    close();
  }
 catch (  IOException e) {
    LOG.warn(e);
  }
  super.doStop();
  for (  Thread thread : _acceptors) {
    if (thread != null)     thread.interrupt();
  }
  int i=_name.lastIndexOf("/");
  if (i > 0)   _name=_name.substring(0,i);
}
