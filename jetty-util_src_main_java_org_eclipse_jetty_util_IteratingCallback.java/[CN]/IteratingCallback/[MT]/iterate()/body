{
  try {
    while (_iterating.compareAndSet(false,true)) {
      if (process()) {
        completed();
        return;
      }
    }
  }
 catch (  Exception e) {
    _iterating.set(false);
    failed(e);
  }
 finally {
    _iterating.set(false);
  }
}
