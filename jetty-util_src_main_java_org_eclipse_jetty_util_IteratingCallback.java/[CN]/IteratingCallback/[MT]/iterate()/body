{
  try {
    while (_iterating.compareAndSet(false,true)) {
      if (process()) {
        _callback.succeeded();
        return;
      }
    }
  }
 catch (  Exception e) {
    _iterating.set(false);
    _callback.failed(e);
  }
 finally {
    _iterating.set(false);
  }
}
