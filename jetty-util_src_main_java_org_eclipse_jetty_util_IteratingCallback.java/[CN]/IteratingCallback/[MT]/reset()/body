{
  while (true) {
switch (_state.get()) {
case IDLE:
      return true;
case SUCCEEDED:
    if (_state.compareAndSet(State.SUCCEEDED,State.IDLE))     return true;
  break;
case FAILED:
if (_state.compareAndSet(State.FAILED,State.IDLE)) return true;
break;
default :
return false;
}
}
}
