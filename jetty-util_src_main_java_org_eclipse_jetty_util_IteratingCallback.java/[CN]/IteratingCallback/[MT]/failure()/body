{
  while (true) {
    State current=_state.get();
    if (current == State.SUCCEEDED) {
      throw new IllegalStateException(toString());
    }
 else {
      if (_state.compareAndSet(current,State.FAILED))       break;
    }
  }
}
