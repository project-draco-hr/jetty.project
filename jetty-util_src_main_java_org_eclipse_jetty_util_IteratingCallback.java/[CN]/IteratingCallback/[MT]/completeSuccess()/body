{
  while (true) {
    State current=_state.get();
    if (current == State.FAILED) {
      return false;
    }
 else {
      if (_state.compareAndSet(current,State.SUCCEEDED))       return true;
    }
  }
}
