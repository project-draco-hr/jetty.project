{
  String d=_dateCache.now();
  int ms=_dateCache.lastMs();
  System.err.println(d + (ms > 99 ? "." : (ms > 0 ? ".0" : ".00")) + ms+ ":WARN:"+ _name+ ":"+ msg);
  if (th != null) {
    if (_hideStacks)     System.err.println(th);
 else     th.printStackTrace();
  }
}
