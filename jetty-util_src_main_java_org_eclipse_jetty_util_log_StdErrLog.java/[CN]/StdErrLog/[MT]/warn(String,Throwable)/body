{
  String d=_dateCache.now();
  int ms=_dateCache.lastMs();
synchronized (_buffer) {
    tag(d,ms,":WARN:");
    format(msg);
    if (_hideStacks)     format(th.toString());
 else     format(th);
    System.err.println(_buffer.toString());
  }
}
