{
  StdErrLog logger=new StdErrLog(fullname);
  logger.setPrintLongNames(_printLongNames);
  logger.setSource(_source);
  logger._stderr=this._stderr;
  if (_level != _configuredLevel)   logger._level=_level;
  return logger;
}
