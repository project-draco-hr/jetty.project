{
  int idxa=getIndexOf(list,fragNameA);
  int idxb=getIndexOf(list,fragNameB);
  if (idxb >= 0 && idxb < idxa) {
    return false;
  }
  if (idxb < 0) {
    if (list == _beforeOthers) {
      return true;
    }
 else     if (list == _afterOthers) {
      if (_beforeOthers.contains(fragNameB))       throw new IllegalStateException("Incorrect relationship: " + fragNameA + " before "+ fragNameB);
 else       return false;
    }
  }
  return true;
}
