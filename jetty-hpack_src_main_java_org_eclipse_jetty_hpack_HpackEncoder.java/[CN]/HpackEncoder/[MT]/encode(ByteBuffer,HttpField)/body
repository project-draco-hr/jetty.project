{
  Entry entry=_context.get(field);
  if (entry != null) {
    if (entry.isInReferenceSet())     return true;
    if (entry.isStatic()) {
      buffer.put((byte)0x00);
      NBitInteger.encode(buffer,4,_context.index(entry));
      buffer.put(entry.getStaticHuffmanValue());
    }
  }
}
