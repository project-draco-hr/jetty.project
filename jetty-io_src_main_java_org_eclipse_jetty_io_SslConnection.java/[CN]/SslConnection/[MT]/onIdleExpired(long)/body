{
  try {
    LOG.debug("onIdleExpired {}ms on {}",idleForMs,this);
    if (_endp.isOutputShutdown())     _appEndPoint.close();
 else     _appEndPoint.shutdownOutput();
  }
 catch (  IOException e) {
    LOG.warn(e);
    super.onIdleExpired(idleForMs);
  }
}
