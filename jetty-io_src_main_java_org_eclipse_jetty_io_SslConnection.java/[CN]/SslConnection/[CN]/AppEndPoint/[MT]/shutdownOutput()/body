{
  _lock.lock();
  try {
    LOG.debug("{} ssl endp.oshut {}",_session,this);
    _engine.closeOutbound();
    _oshut=true;
    process(null);
  }
 catch (  IOException e) {
    LOG.debug(e);
  }
 finally {
    _lock.unlock();
  }
}
