{
  long now=System.currentTimeMillis();
  for (  SelectionKey key : _selector.keys()) {
    Object attachment=key.attachment();
    if (attachment instanceof AsyncEndPoint)     ((AsyncEndPoint)attachment).checkTimeout(now);
  }
}
