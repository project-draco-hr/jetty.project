{
  if (__submitKeyUpdates)   submit(update);
 else {
    update.run();
    out:     while (true) {
switch (_state.get()) {
case SELECT:
        if (!_state.compareAndSet(State.SELECT,State.WAKEUP))         continue;
      wakeup();
    break out;
default :
  break out;
}
}
}
}
