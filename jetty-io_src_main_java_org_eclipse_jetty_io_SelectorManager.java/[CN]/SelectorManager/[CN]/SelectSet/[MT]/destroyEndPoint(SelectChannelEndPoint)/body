{
  LOG.debug("destroyEndPoint {}",endp);
  _endPoints.remove(endp);
  AbstractAsyncConnection connection=endp.getAsyncConnection();
  if (connection != null)   connection.onClose();
  endPointClosed(endp);
}
