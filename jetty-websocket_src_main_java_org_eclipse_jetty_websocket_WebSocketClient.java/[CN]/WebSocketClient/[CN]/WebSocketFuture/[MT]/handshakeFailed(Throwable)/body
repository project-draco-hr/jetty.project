{
  try {
    ByteChannel channel=null;
synchronized (this) {
      if (_channel != null) {
        channel=_channel;
        _channel=null;
        _exception=ex;
      }
    }
    if (channel != null) {
      if (ex instanceof ProtocolException)       closeChannel(channel,WebSocketConnectionD11.CLOSE_PROTOCOL,ex.getMessage());
 else       closeChannel(channel,WebSocketConnectionD11.CLOSE_NOCLOSE,ex.getMessage());
    }
  }
  finally {
    _done.countDown();
  }
}
