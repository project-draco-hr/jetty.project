{
  if (!(isStarted() || isStarting()))   _durableListeners=LazyList.add(_durableListeners,listener);
  setEventListeners((EventListener[])LazyList.addToArray(getEventListeners(),listener,EventListener.class));
}
