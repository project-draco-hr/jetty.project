{
  getShell().getDisplay().asyncExec(new Runnable(){
    public void run(){
      setDefaultWorkingDirectoryText(_configAreaHolder.getConfigurationAreaLocation() + File.separator + "jettyhome");
      _initializing=false;
    }
  }
);
}
