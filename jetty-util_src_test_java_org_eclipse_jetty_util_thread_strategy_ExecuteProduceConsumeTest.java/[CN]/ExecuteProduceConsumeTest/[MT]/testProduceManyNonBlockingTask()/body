{
  Task[] tasks=new Task[10];
  for (int i=0; i < tasks.length; i++) {
    tasks[i]=new Task();
    _produce.add(tasks[i]);
  }
  _produce.add(NULLTASK);
  _ewyk.execute();
  for (  Task task : tasks)   assertThat(task.hasRun(),equalTo(true));
  Assert.assertEquals(_ewyk,_executions.poll());
}
