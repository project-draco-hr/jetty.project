{
  super(protocol);
  this.protocols=new ArrayList<>();
  if (protocols != null) {
    for (    String p : protocols) {
      p=p.trim();
      if (!p.isEmpty())       this.protocols.add(p.trim());
    }
  }
}
