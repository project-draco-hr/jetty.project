{
synchronized (this) {
    if (_acceptChannel != null) {
      removeBean(_acceptChannel);
      try {
        if (_acceptChannel.isOpen())         _acceptChannel.close();
      }
 catch (      IOException e) {
        LOG.warn(e);
      }
    }
    _acceptChannel=null;
    _localPort=-2;
  }
}
