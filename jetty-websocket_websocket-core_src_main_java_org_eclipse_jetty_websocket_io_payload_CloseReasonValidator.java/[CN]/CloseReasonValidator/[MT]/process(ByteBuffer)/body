{
  if ((payload == null) || (payload.remaining() <= 2)) {
    return;
  }
  ByteBuffer copy=payload.slice();
  while (statusCodeBytes > 0) {
    copy.get();
    statusCodeBytes--;
  }
  super.process(copy);
}
