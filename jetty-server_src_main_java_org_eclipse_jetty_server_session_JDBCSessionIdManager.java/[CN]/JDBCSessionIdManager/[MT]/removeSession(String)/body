{
  if (id == null)   return;
synchronized (_sessionIds) {
    if (__log.isDebugEnabled())     __log.debug("Removing session id=" + id);
    try {
      _sessionIds.remove(id);
      delete(id);
    }
 catch (    Exception e) {
      __log.warn("Problem removing session id=" + id,e);
    }
  }
}
