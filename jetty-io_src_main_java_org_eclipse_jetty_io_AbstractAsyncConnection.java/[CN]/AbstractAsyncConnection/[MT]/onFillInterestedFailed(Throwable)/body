{
  LOG.debug("{} onFillInterestedFailed {}",this,cause);
  if (_endp.isOpen()) {
    boolean close=true;
    if (cause instanceof TimeoutException)     close=onReadTimeout();
    if (close) {
      if (_endp.isOutputShutdown())       _endp.close();
 else       _endp.shutdownOutput();
    }
  }
}
