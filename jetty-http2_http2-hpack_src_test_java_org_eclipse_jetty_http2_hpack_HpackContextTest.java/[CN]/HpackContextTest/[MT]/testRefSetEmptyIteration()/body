{
  HpackContext ctx=new HpackContext(4096);
  for (  Entry entry : ctx.getReferenceSet())   fail("unexpected:" + entry);
  Iterator<Entry> iter=ctx.iterateReferenceSet();
  assertFalse(iter.hasNext());
  try {
    iter.next();
    fail();
  }
 catch (  NoSuchElementException e) {
  }
  try {
    iter.remove();
    fail();
  }
 catch (  NoSuchElementException e) {
  }
}
