{
  List<String> ret=new ArrayList<>();
  if (extensions == null) {
    return ret;
  }
  for (  Extension ext : extensions) {
    ret.add(ext.getConfig().getParameterizedName());
  }
  return ret;
}
