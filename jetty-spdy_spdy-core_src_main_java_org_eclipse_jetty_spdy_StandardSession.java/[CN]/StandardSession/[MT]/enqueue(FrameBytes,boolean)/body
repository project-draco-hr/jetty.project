{
synchronized (queue) {
    int index=0;
    while (index < queue.size()) {
      FrameBytes element=queue.get(index);
      int comparison=element.compareTo(frameBytes);
      if (comparison > 0 || prepend && comparison == 0)       break;
      ++index;
    }
    queue.add(index,frameBytes);
  }
}
