{
  try {
    if (handler != null)     handler.failed(context,x);
  }
 catch (  Exception xx) {
    logger.info("Exception while notifying handler " + handler,xx);
  }
catch (  Error xx) {
    logger.info("Exception while notifying handler " + handler,xx);
    throw xx;
  }
}
