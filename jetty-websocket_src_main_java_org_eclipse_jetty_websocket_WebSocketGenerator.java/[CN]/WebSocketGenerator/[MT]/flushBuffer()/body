{
  if (!_endp.isOpen())   throw new IOException("Closed");
  if (_buffer != null) {
    int flushed=_endp.flush(_buffer);
    if (flushed > 0)     _buffer.skip(flushed);
    return flushed;
  }
  return 0;
}
