{
synchronized (this) {
    if (!_invalid) {
      if (_lastAccessed + _maxIdleMs < time) {
        invalidate();
      }
 else {
        _newSession=false;
        _lastAccessed=_accessed;
        _accessed=time;
        _requests++;
      }
    }
  }
}
