{
  boolean result=true;
  for (  Response.ResponseListener listener : listeners)   if (listener instanceof Response.HeaderListener)   result&=notifyHeader((Response.HeaderListener)listener,response,field);
  return result;
}
