{
  HttpExchange exchange=new ContentExchange(){
    @Override protected void onException(    Throwable x){
      if (!(x instanceof ProtocolException))       super.onException(x);
    }
  }
;
  sendRequestThroughProxy(exchange,"error500",9);
}
