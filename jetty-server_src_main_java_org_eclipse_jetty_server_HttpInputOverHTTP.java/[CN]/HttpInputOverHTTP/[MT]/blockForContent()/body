{
  while (true) {
    _httpConnection.fillInterested(_readBlocker);
    LOG.debug("{} block readable on {}",this,_readBlocker);
    _readBlocker.block();
    if (nextContent() != null || isFinished())     break;
  }
}
