{
  for (  EchoBroadcastSocket sock : BROADCAST) {
    try {
      sock.conn.write(buf,offset,len);
    }
 catch (    IOException e) {
      BROADCAST.remove(sock);
      e.printStackTrace();
    }
  }
}
