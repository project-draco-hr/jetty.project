{
  Set<Session> sessions=clientSessions.remove(serverSession);
  if (sessions != null) {
    for (    Session session : sessions)     session.goAway(getTimeout(),TimeUnit.MILLISECONDS,new Handler.Adapter<Void>());
  }
}
