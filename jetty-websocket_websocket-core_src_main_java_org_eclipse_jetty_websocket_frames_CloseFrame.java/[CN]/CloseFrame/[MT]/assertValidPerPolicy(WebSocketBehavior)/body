{
  int code=getStatusCode();
  if ((code < StatusCode.NORMAL) || (code == StatusCode.UNDEFINED) || (code == StatusCode.NO_CLOSE)|| (code == StatusCode.NO_CODE)|| ((code > 1011) && (code <= 2999))|| (code >= 5000)) {
    throw new ProtocolException("Invalid close code: " + code);
  }
}
