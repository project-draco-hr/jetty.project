{
  try {
    conn.write(message);
    long iter=count.incrementAndGet();
    if ((iter % 100) == 0) {
      LOG.info("Echo'd back {} msgs",iter);
    }
  }
 catch (  IOException e) {
    e.printStackTrace(System.err);
  }
}
