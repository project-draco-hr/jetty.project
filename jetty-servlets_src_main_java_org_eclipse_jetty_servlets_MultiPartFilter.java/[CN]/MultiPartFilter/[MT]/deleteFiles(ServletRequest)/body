{
  ArrayList<File> files=(ArrayList<File>)request.getAttribute(FILES);
  if (files != null) {
    for (    File file : files) {
      try {
        file.delete();
      }
 catch (      Exception e) {
        _context.log("failed to delete " + file,e);
      }
    }
  }
}
