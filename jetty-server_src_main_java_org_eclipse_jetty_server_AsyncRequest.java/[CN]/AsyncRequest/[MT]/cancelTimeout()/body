{
  EndPoint endp=_connection.getEndPoint();
  if (endp.isBlocking()) {
synchronized (this) {
      _timeoutMs=0;
      this.notifyAll();
    }
  }
 else   if (_event != null)   _connection.cancelTimeout(_event._timeout);
}
