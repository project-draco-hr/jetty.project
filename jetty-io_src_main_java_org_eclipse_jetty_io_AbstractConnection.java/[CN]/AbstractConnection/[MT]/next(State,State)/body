{
  if (next == null)   return true;
  if (_state.compareAndSet(state,next)) {
    LOG.debug("{}-->{} {}",state,next,this);
    if (next != state)     next.onEnter(AbstractConnection.this);
    return true;
  }
  return false;
}
