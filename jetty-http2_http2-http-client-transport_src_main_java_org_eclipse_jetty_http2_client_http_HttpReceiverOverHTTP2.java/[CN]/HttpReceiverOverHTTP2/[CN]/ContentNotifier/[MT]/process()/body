{
  DataInfo dataInfo;
synchronized (this) {
    dataInfo=queue.poll();
  }
  if (dataInfo == null)   return Action.IDLE;
  this.dataInfo=dataInfo;
  responseContent(dataInfo.exchange,dataInfo.buffer,this);
  return Action.SCHEDULED;
}
