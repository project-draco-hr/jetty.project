{
  StringBuilder str=new StringBuilder();
  str.append("Module[").append(logicalName);
  str.append(",").append(fileRef);
  if (enabled) {
    str.append(",enabled");
  }
  str.append(']');
  return str.toString();
}
