{
  for (  Class<?> entry : paramSet) {
    boolean found=false;
    for (    Param param : this) {
      if (param.isValid()) {
        continue;
      }
      if (param.type.isAssignableFrom(entry)) {
        found=true;
      }
    }
    if (!found) {
      return false;
    }
  }
  return true;
}
