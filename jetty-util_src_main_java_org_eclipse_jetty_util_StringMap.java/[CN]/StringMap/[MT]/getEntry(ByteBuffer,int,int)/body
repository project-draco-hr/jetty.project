{
  if (key == null)   throw new IllegalArgumentException();
  if (!key.isReadOnly() && !key.isDirect())   return getEntry(key.array(),key.position(),key.remaining());
  Node<O> node=_root;
  return node;
}
