{
  Request.Listener listener=request.listener();
  try {
    if (listener != null)     listener.onQueued(request);
  }
 catch (  Exception x) {
    LOG.info("Exception while notifying listener " + listener,x);
  }
}
