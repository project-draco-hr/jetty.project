{
  LOG.debug("{} released",connection);
  if (client.isRunning()) {
    activeConnections.remove(connection);
    process(connection);
  }
 else {
    LOG.debug("{} is stopped",client);
    remove(connection);
    connection.close();
  }
}
