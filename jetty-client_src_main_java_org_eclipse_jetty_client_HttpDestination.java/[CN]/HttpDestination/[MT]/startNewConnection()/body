{
  try {
synchronized (this) {
      _pendingConnections++;
    }
    _client._connector.startConnection(this);
  }
 catch (  Exception e) {
    e.printStackTrace();
    onConnectionFailed(e);
  }
}
