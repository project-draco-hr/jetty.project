{
  if (_value == null)   return false;
  if (value.equalsIgnoreCase(_value))   return true;
  String[] split=__splitter.split(_value);
  for (int i=0; split != null && i < split.length; i++) {
    if (value.equalsIgnoreCase(split[i]))     return true;
  }
  return false;
}
