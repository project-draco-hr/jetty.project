{
  boolean execute=true;
  while (execute) {
    try {
      LOG.debug("Executing {}",this);
      super.run();
    }
  finally {
      LOG.debug("Completing {}",this);
synchronized (this) {
        dispatched=redispatch;
        redispatch=false;
        execute=dispatched;
      }
    }
  }
}
