{
  boolean proceed=performBeginRequest(headers);
  if (!proceed)   return;
  performHeaders(headers);
  if (endRequest) {
    if (headerComplete())     post(this);
    if (messageComplete())     post(this);
  }
}
