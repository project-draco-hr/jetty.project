{
synchronized (lock()) {
    try {
      while (!isFinished()) {
        Content item=getNextContent();
        if (item == null)         _contentState.waitForContent(this);
 else         consume(item,remaining(item));
      }
    }
 catch (    IOException e) {
      LOG.debug(e);
    }
  }
}
