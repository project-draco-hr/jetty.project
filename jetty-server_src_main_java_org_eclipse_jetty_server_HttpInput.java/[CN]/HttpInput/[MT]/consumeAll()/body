{
  while (true) {
synchronized (lock()) {
      _inputQ.clear();
    }
    if (_inputEOF)     return;
    try {
      blockForContent();
    }
 catch (    IOException e) {
      LOG.warn(e);
    }
  }
}
