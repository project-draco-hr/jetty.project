{
synchronized (lock()) {
    if (!isEOF()) {
      LOG.debug("{} early EOF",this);
      _eofState=EARLY_EOF;
      if (_listener == null)       return;
    }
  }
  _channelState.onReadPossible();
}
