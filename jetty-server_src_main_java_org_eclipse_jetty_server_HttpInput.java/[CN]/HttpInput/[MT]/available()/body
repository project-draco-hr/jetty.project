{
  try {
synchronized (lock()) {
      T item=nextContent();
      if (item == null) {
        checkEOF();
        return 0;
      }
      return remaining(item);
    }
  }
 catch (  IOException e) {
    throw new RuntimeIOException(e);
  }
}
