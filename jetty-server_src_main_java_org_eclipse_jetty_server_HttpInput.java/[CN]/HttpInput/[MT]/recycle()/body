{
synchronized (_inputQ.lock()) {
    _inputEOF=false;
    if (_content != null)     onContentConsumed(_content);
    while ((_content=_inputQ.poll()) != null)     onContentConsumed(_content);
    if (_content != null)     onAllContentConsumed();
    _content=null;
  }
}
