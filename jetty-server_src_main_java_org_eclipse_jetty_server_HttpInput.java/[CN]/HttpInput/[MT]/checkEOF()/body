{
  if (_eof != null) {
    LOG.debug("{} eof {}",this,_eof);
    _state=_eof;
    _eof=null;
    if (_listener != null) {
      try {
        _listener.onAllDataRead();
      }
 catch (      Exception e) {
        LOG.warn(e.toString());
        LOG.debug(e);
        _listener.onError(e);
      }
    }
  }
}
