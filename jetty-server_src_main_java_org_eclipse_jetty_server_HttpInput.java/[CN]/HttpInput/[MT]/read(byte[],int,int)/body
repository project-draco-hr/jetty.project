{
  T item=null;
synchronized (lock()) {
    item=getNextContent();
    if (item == null) {
      _state.waitForContent(this);
      item=getNextContent();
      if (item == null)       return _state.noContent();
    }
  }
  return get(item,b,off,len);
}
