{
synchronized (_inputQ) {
    Content item=nextContent();
    if (item == null && _state.blockForContent(this))     item=nextContent();
    if (item == null)     return _state.noContent();
    int l=get(item,b,off,len);
    return l;
  }
}
