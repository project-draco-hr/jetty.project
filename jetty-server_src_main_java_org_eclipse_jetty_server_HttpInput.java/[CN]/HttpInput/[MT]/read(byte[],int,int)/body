{
  int l=-1;
  if (BufferUtil.isEmpty(_content))   _content=_connection.blockForContent();
  if (BufferUtil.hasContent(_content)) {
    l=Math.min(len,_content.remaining());
    _content.get(b,off,l);
  }
  return l;
}
