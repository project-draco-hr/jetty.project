{
  T item=null;
synchronized (lock()) {
    item=nextContent();
    if (item == null) {
      checkEOF();
      _state.waitForContent(this);
      item=nextContent();
      if (item == null) {
        checkEOF();
        return _state.noContent();
      }
    }
  }
  return get(item,b,off,len);
}
