{
  T item=null;
  int l;
synchronized (lock()) {
    item=getNextContent();
    if (item == null) {
      _state.waitForContent(this);
      item=getNextContent();
      if (item == null)       return _state.noContent();
    }
    l=get(item,b,off,len);
    _contentRead+=l;
  }
  return l;
}
