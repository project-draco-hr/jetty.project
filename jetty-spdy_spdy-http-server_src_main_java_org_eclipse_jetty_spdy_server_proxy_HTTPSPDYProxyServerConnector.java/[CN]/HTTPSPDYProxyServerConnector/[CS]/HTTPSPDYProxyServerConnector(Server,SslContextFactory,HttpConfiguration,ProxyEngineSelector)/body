{
  super(server,sslContextFactory,sslContextFactory == null ? new ConnectionFactory[]{new ProxyHTTPConnectionFactory(config,SPDY.V2,proxyEngineSelector)} : new ConnectionFactory[]{new NPNServerConnectionFactory("spdy/3","spdy/2","http/1.1"),new HTTPSPDYServerConnectionFactory(SPDY.V3,config),new HTTPSPDYServerConnectionFactory(SPDY.V2,config),new ProxyHTTPConnectionFactory(config,SPDY.V2,proxyEngineSelector)});
  NPNServerConnectionFactory npnConnectionFactory=getConnectionFactory(NPNServerConnectionFactory.class);
  if (npnConnectionFactory != null)   npnConnectionFactory.setDefaultProtocol("http/1.1");
}
