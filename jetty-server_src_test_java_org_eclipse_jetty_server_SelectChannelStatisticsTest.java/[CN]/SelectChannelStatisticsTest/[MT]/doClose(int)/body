{
  for (int idx=0; idx < count; idx++) {
    if (_socket[idx] != null) {
      System.err.println("Closing " + _socket[idx]);
      _socket[idx].close();
    }
  }
  System.err.println("Close wait");
  _closed.await();
}
