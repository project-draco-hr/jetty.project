{
  super.failed(v,x);
  if (x instanceof EofException) {
    LOG.warn("failed(" + v + ") - "+ EofException.class);
  }
 else {
    LOG.warn("failed(" + v + ")",x);
  }
  cancelTask();
  frame.notifySendFailed(x);
}
