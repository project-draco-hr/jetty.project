{
  Handler old=getHandler();
  if (old == null)   setHandler(handler);
 else   if (old instanceof HandlerWrapper)   ((HandlerWrapper)old).appendHandler(handler);
 else   if (old instanceof HandlerCollection)   ((HandlerCollection)old).addHandler(handler);
 else   throw new IllegalStateException();
}
