{
  if (getHandler() == null)   setHandler(handler);
 else   if (getHandler() instanceof HandlerCollection)   ((HandlerCollection)getHandler()).addHandler(handler);
 else {
    HandlerCollection collection=new HandlerCollection();
    collection.setHandlers(new Handler[]{getHandler(),handler});
    setHandler(collection);
  }
}
