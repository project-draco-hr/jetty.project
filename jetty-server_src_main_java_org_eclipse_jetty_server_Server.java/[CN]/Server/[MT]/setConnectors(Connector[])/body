{
  if (connectors != null) {
    for (int i=0; i < connectors.length; i++) {
      if (connectors[i].getServer() != this)       throw new IllegalArgumentException();
    }
  }
  _container.update(this,_connectors,connectors,"connector");
  _connectors=connectors;
}
