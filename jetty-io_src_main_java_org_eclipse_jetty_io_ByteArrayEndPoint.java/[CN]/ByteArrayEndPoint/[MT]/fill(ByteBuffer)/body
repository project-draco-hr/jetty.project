{
  if (_closed)   throw new IOException("CLOSED");
  if (_in == null)   shutdownInput();
  if (_ishut)   return -1;
  int filled=BufferUtil.append(_in,buffer);
  if (filled > 0)   notIdle();
  return filled;
}
