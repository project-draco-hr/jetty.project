{
  if (_closed)   throw new IOException("CLOSED");
  int len=0;
  for (  ByteBuffer b : buffers) {
    if (b.hasRemaining()) {
      int l=flush(b);
      if (l > 0)       len+=l;
 else       break;
    }
  }
  return len;
}
