{
  ByteBuffer b;
  try (Locker.Lock lock=_locker.lock()){
    if (BufferUtil.isEmpty(_out))     _hasOutput.await(time,unit);
    b=_out;
    _out=BufferUtil.allocate(b.capacity());
  }
   getWriteFlusher().completeWrite();
  return b;
}
