{
  Object[][] data=new Object[][]{{new TimerScheduler()},{new ScheduledExecutionServiceScheduler()},{new ConcurrentScheduler(0)},{new ConcurrentScheduler(1500)},{new ConcurrentScheduler(executor,1500)}};
  return Arrays.asList(data);
}
