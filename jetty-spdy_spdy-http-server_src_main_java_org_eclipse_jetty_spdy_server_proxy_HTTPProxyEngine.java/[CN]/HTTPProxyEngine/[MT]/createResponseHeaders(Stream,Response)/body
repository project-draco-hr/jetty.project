{
  Fields responseHeaders=new Fields();
  for (  HttpField header : response.getHeaders())   responseHeaders.add(header.getName(),header.getValue());
  addResponseProxyHeaders(clientStream,responseHeaders);
  return responseHeaders;
}
