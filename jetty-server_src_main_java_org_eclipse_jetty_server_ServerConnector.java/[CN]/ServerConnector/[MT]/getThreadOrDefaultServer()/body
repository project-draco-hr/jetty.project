{
  Server server=_threadServer.get();
  if (server == null)   server=_defaultServer;
  if (server == null)   throw new IllegalStateException("setDefaultServer or setThreadServer must be called before constructor ServerConnector()");
  return server;
}
