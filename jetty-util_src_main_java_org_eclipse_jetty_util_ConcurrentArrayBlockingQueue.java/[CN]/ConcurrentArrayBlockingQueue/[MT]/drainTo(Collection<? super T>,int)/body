{
  if (c == this)   throw new IllegalArgumentException();
  int added=0;
  while (added < maxElements) {
    T element=poll();
    if (element == null)     break;
    c.add(element);
    ++added;
  }
  return added;
}
